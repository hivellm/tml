// Test simple closure syntax - inline use

func apply(x: I32) -> I32 {
    // Use closure directly without storing
    let f: func(I32) -> I32 = do(y: I32) -> I32 y + 1
    return f(x)
}

func main() {
    let result: I32 = apply(5)
    println(result)
}
