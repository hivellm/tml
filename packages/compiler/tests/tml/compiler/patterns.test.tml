use test

type Maybe[T] {
    Just(T),
    Nothing,
}

type Outcome[T, E] {
    Ok(T),
    Err(E),
}

@test
func pattern_binding_let() -> I32 {
    let x: I32 = 42
    assert_eq_i32(x, 42, "x should equal 42")
    return 0
}

@test
func pattern_maybe() -> I32 {
    let x: Maybe[I32] = Just(42)
    // Pattern matching with when is tested here
    // For now, just verify the type compiles
    return 0
}

@test
func pattern_outcome() -> I32 {
    let result: Outcome[I32, I32] = Ok(200)
    // Pattern matching with when expression
    // Verifies the type system accepts the pattern
    return 0
}