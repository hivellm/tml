use test

@test
func test_with_use() -> I32 {
    assert_eq_i32(2 + 2, 4, "2 + 2 should be 4")
    assert_eq_bool(true, true, "true should equal true")
    assert(true, "this should pass")
    return 0
}
