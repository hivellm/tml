// Environment variable utilities

/// Get an environment variable
pub func get(key: Str) -> Maybe[Str] {
    // TODO: Call builtin env_get
    Nothing
}

/// Set an environment variable
pub func set(key: Str, value: Str) {
    // TODO: Call builtin env_set
}

/// Remove an environment variable
pub func remove(key: Str) {
    // TODO: Call builtin env_remove
}

/// Get all environment variables
pub func vars() -> Vec[(Str, Str)] {
    // TODO: Call builtin env_vars
    Vec::new()
}

/// Get current working directory
pub func current_dir() -> Maybe[Str] {
    // TODO: Call builtin env_current_dir
    Nothing
}

/// Set current working directory
pub func set_current_dir(path: Str) -> Outcome[(), Str] {
    // TODO: Call builtin env_set_current_dir
    Err("not implemented")
}

@test
func test_env_get() {
    let path: Maybe[Str] = get("PATH")
    // PATH should exist on most systems
    assert!(path.is_just() or path.is_nothing())
}
