// Path manipulation

/// File path
pub type Path {
    path: Str,
}

impl Path {
    /// Create from string
    pub func new(s: Str) -> Path {
        return Path { path: s }
    }

    /// Get as string
    pub func as_str(this) -> Str {
        return this.path
    }

    /// Get file name
    pub func file_name(this) -> Maybe[Str] {
        // TODO: Implement
        return Nothing
    }

    /// Get extension
    pub func extension(this) -> Maybe[Str] {
        // TODO: Implement
        return Nothing
    }

    /// Get parent directory
    pub func parent(this) -> Maybe[Path] {
        // TODO: Implement
        return Nothing
    }

    /// Join with another path
    pub func join(this, other: Str) -> Path {
        // TODO: Implement proper path joining
        return Path::new(this.path + "/" + other)
    }

    /// Check if absolute
    pub func is_absolute(this) -> Bool {
        // TODO: Implement
        return false
    }

    /// Check if relative
    pub func is_relative(this) -> Bool {
        return not this.is_absolute()
    }
}
