// TML Standard Library - Math Module
// Comprehensive mathematical functions library based on Go's math package
// All functions use F64 (double precision) like Go's math package

// ============ CONSTANTS ============

pub use const::{
    // Mathematical constants
    E, Pi, Phi,
    Sqrt2, SqrtE, SqrtPi, SqrtPhi,
    Ln2, Log2E, Ln10, Log10E,

    // Floating-point limits
    MaxFloat64, SmallestNonzeroFloat64,
    MaxFloat32, SmallestNonzeroFloat32,

    // Integer limits
    MaxInt8, MinInt8, MaxUint8,
    MaxInt16, MinInt16, MaxUint16,
    MaxInt32, MinInt32,
    MaxInt64, MinInt64,

    // Conversion constants
    DegToRad, RadToDeg
}

// ============ BASIC ARITHMETIC ============

pub use abs::{abs}
pub use minmax::{min, max, clamp}
pub use dim::{dim}
pub use copysign::{copysign, signbit}
pub use fmod::{mod, remainder}

// ============ ROUNDING ============

pub use floor::{floor}
pub use ceil::{ceil}
pub use round::{round, round_to_even}
pub use trunc::{trunc}

// ============ POWER & ROOT ============

pub use sqrt::{sqrt}
pub use cbrt::{cbrt}
pub use pow::{pow, powi, pow10}
pub use hypot::{hypot}

// ============ EXPONENTIAL ============

pub use exp::{exp, exp2, expm1}

// ============ LOGARITHMIC ============

pub use log::{log, log2, log10, log1p, logb, ilogb}

// ============ TRIGONOMETRIC ============

pub use sin::{sin}
pub use cos::{cos, sincos}
pub use tan::{tan}

pub use asin::{asin}
pub use acos::{acos}
pub use atan::{atan, atan2}

// ============ HYPERBOLIC ============

pub use sinh::{sinh}
pub use cosh::{cosh}
pub use tanh::{tanh}

pub use asinh::{asinh}
pub use acosh::{acosh}
pub use atanh::{atanh}

// ============ SPECIAL FUNCTIONS ============

pub use gamma::{gamma, lgamma}
pub use erf::{erf, erfc, erfinv, erfcinv}
pub use bessel::{j0, j1, jn, y0, y1, yn}

// ============ FLOATING-POINT MANIPULATION ============

pub use frexp::{frexp}
pub use ldexp::{ldexp}
pub use modf::{modf}
pub use fma::{fma}
// Note: nextafter and nextafter32 are available as global builtins

// ============ BIT OPERATIONS ============
// Note: float32_bits, float32_from_bits, float64_bits, float64_from_bits
// are available as global builtins - no import needed

// ============ SPECIAL VALUES ============
// Note: infinity, nan, is_inf, is_nan are available as global builtins
// inf() wrapper provided for Go compatibility

pub func inf(sign: I32) -> F64 {
    return infinity(sign)
}
