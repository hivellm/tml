// Copysign returns a value with the magnitude of f and the sign of sign

use abs

pub func copysign(f: F64, sign: F64) -> F64 {
    let magnitude: F64 = abs(f)
    return if sign < 0.0 then -magnitude else magnitude
}

pub func signbit(x: F64) -> Bool {
    return x < 0.0
}
