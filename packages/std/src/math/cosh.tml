// Hyperbolic cosine function

use exp

// cosh(x) = (e^x + e^-x) / 2
pub func cosh(x: F64) -> F64 {
    let exp_x: F64 = exp(x)
    let exp_neg_x: F64 = exp(-x)

    return (exp_x + exp_neg_x) / 2.0
}
