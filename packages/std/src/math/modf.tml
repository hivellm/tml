// Modf returns integer and fractional floating-point numbers

use trunc

pub func modf(f: F64) -> (F64, F64) {
    let int_part: F64 = trunc(f)
    let frac_part: F64 = f - int_part

    return (int_part, frac_part)
}
