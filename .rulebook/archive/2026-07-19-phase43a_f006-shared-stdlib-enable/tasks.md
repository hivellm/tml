## 1. Implementation
- [x] 1.1 Reproduce the fast-path failure on a scratch re-enable of `build_stdlib_object` (`testing_compile_parallel.cpp`), capture each error class with IR/linker evidence to `.sandbox/` (mem::replace dangling, I32::duplicate, i64/i32 range width, phantom-pub-mod bootstrap abort). Confirm the four root causes hold against current source — CONFIRMED against source (phase41b already reproduced with evidence in `.sandbox/repro_hash5.log` + `04-test-framework-performance.md` F-006). Re-verified each root cause holds in current source: Fix-B gap present at `call_user.cpp` (no free-func re-mangle branch), dedup capture gap present (`generate_support.cpp` captures only `generated_functions_`), phantom `pub mod` in `test_bootstrap.tml`, `loop.cpp` already i64-promotes for-range (blocker 3 residual = Range-as-value path).
- [x] 1.2 Blocker 1 — generic free-function monomorphization (Fix B): in `call_user.cpp` after `free_func_type_subs`, when `func_sig->type_params` non-empty AND all concrete, `require_func_instantiation(bare, type_args)` + override the mangled callee (mirror `Type::method` at 595-632); OR fix upstream in `gen_call_generic_func`. Verify the `mem::replace[T]` dangling ref disappears — LANDED. Added the module-free-function branch in `call_user.cpp` (guarded `!is_type_method`, type_params non-empty, all-concrete, rejects bare-typeparam subs). Fires only on the `gen_call_generic_func` miss path → inert on the passing path + in the shipped fast-path-off build. Full dangling-ref-gone verification requires the fast-path (blocker 4) to engage; safe to land now (mirrors the proven Type::method pattern).
- [x] 1.3 Blocker 2 — dedup keyspace: capture+restore `generated_impl_methods_output_` + `generated_impl_methods_` in `CodegenLibraryState` (`generate_support.cpp:622`, `generate.cpp:705-707`); verify I32::duplicate redefinition gone — LANDED. Added both sets to `CodegenLibraryState` (`llvm_ir_gen.hpp`), capture (`generate_support.cpp`), restore (`generate.cpp`). Clean fix; full I32::duplicate-gone verification requires the fast-path to engage.
- [x] 1.4 Blocker 3 — CLOSED AS MISDIAGNOSED. The fast-path was reproduced (via `tml test --unified`, which routes through the path that already calls `build_stdlib_object` — no code edit needed) and the recorded hypothesis is DISPROVEN: the actual error is `ptr` vs `i32`, not the `Range__I32`-vs-`Range__I64` i64/i32 width. The register name in the old note (`%t9030`) is not stable across builds — now `%t11353` — so it is not an identifier and must not be chased. Root cause established: blocker 3 IS blocker 4 (see 1.5). No separate range-width fix exists to land; a codegen patch here would have been fixing a bug that was never there.
- [x] 1.5 Blocker 4 — RESOLVED via "Option B" (SESSION 3), a better design than the scoped bootstrap the task originally assumed. Instead of scoping WHICH modules get eagerly compiled (which reduces but never eliminates unexercised-body compilation — eligibility is per-module, not per-function), the bootstrap now emits ZERO bodies: a new `library_skip_user_code` option keeps lazy deferral active, `CodegenLibraryState` captures the two pending-body maps (AST ptr + module context, hard-gated per entry on `GlobalASTCache::should_cache` — measured skipped=0), and workers restore them so the already-wired `emit_referenced_library_definitions()` emits exactly the referenced set. The treadmill dies BY CONSTRUCTION; the Str-temp auto-free hazard is structurally eliminated (this path sets `in_library_body_=true`); the never-linked stdlib `.obj` + its mtime scan are deleted. Bootstrap ~16min(failing)→~34s(working).

  NINE root-cause fixes were needed to reach green (all found by reproduction, none guessed — details in `docs/patches/v0.3.79.md`): conditional runtime types mirrored in workers; emission-ledger truth filter (the bootstrap claimed 1836 generated bodies, only 4 had captured text); vtable body-availability check; symbol-divergence alias bridge; demangling recovery for skipped call-site queues; JSON runtime declare dedup (dso_local); **deterministic `find_module_for_type` — THE ROOT of the entire divergence class** (module identity for re-exported types was chosen by unordered_map iteration order, diverging between bootstrap and worker instances); the supplemental module scan (the safety net `processed_module_paths` promised but never had — catches modules the bootstrap's use-driven loading missed, e.g. `std::json::serialize`); the dead UNRESOLVED warning made live.

  AMORTIZATION: the state holds AST pointers → cannot persist to disk → ~30s bootstrap per PROCESS. Enabled only for runs with ≥32 files; `TML_STDLIB_FASTPATH=1|0` forces (the gate runs forced ON). Small targeted runs keep the normal path — dev-loop UX unchanged, full runs amortize once.

  ROOT CAUSE (verified by reproduction): with `library_ir_only=true` the lazy-deferral guards at `decl/impl.cpp:411` and `decl/func.cpp:647` are disabled, so the stdlib pass abandons reference-driven emission and eagerly compiles ~5000 library bodies that nothing has ever exercised. Codegen defects in never-compiled code surface only here — that is the entire mechanism behind the "K001 under the fast-path" class, including what was recorded as blocker 3. Patching the individual offending body is a treadmill: the next unexercised body fails behind it. Side effect: the flush loop at `generate_library_only.cpp:147-238` is dead code in this mode (the same flag makes its stated job impossible).

  TWO SUB-BLOCKERS RESOLVED THIS SESSION (they gated everything else — nothing behind them had ever run):
    - Phantom bootstrap imports: 12 modules named by non-existent paths (`use core::mem` vs the real `core::runtime::mem`, `core::clone` vs `core::traits::clone`, `std::datetime` vs `std::time::datetime`, …). The first one aborted typecheck → `build_stdlib_object` returned `""` → `g_stdlib_codegen_state` null → fast path never engaged. FIXED; all 12 verified against disk and against real in-tree usage before editing.
    - `Unknown method: send_once`: private methods of non-generic/non-behavior impl blocks were skipped by the visibility filter in `env_module_load_decls.cpp`, so `HttpClient::send` -> `send_once` had no FuncSig. FIXED. This is a LATENT GENERAL bug, not a fast-path bug — normal compilation defers library bodies until referenced and no test calls `HttpClient::send`.

  MEASURED: eager bootstrap = 968s with `std::http`, 449s without (the old note's ">500s" estimate; an earlier 939s figure was time-to-ABORT at `send_once`, not a completion). Fast path confirmed ENGAGING for the first time ("Stdlib codegen state available").

  TREADMILL DEMONSTRATED EMPIRICALLY (this is the evidence that closes the "just patch the body" option): three successive runs, each clearing the then-current blocker, each surfaced a DIFFERENT K001 in a DIFFERENT never-exercised body — (1) full set → `Unknown method: send_once` (fixed); (2) `std::http` excluded → `'%t11353' ... 'ptr' but expected 'i32'`; (3) full set with `send_once` fixed → `'%t4937' ... 'i64' but expected '%struct.RawThread = type { i64 }'`. Different module sets surface different latent defects and each fix exposes the next body behind it.

  TRAP for the implementer: the codegen STATE is captured and the run logs "Stdlib codegen state available (fast path enabled)" EVEN WHEN the stdlib object FAILED to compile — capture is independent of object compilation. Do not treat that log line as evidence the shared object is valid.

  STRUCTURAL NOTE for whoever implements this: `group_into_suites` uses `max_per_suite = 1`, so a suite is effectively ONE FILE. A literally per-suite bootstrap would build N bootstraps instead of 1 and be SLOWER — the state cache must be keyed on a module-set hash so files with identical import sets share. `g_stdlib_codegen_state`/`g_stdlib_obj_path` are single process-globals and would need to become a keyed map with its own synchronization (up to 8 concurrent suite workers).

  RELATED LATENT DEFECT found while diagnosing (worth its own task): the eager emissions run with `in_library_body_ == false`, enabling Phase 4b Str-temp auto-free inside library bodies — precisely what that flag exists to prevent ("Auto-freeing those temps causes use-after-free", `llvm_ir_gen.hpp`).
- [x] 1.6 Enable the fast-path — DONE (SESSION 3): enabled with the ≥32-file amortization heuristic + `TML_STDLIB_FASTPATH` override. The `library_decls_only=true` half (F-007: decls-only test objects + shared object with EXTERNAL linkage) remains FUTURE WORK — its economics changed under Option B (test objects now embed only the referenced bodies, not the full ~5000, so the residual win is smaller); `generate_library_only.cpp` still emits no linkage attribute, which is the concrete F-007 work item. Left with F-007 in `04-test-framework-performance.md`.
- [x] 1.7 GATE: PASSED IN FULL (SESSION 3), both modes, `--no-cache`. Fast-path FORCED ON: core/hash 14/14, core/str 33/33, std/json 23/23, compiler/borrow 12/12, core/alloc 43/44 (documented floating concurrency miss, re-run confirmed at baseline). Fast-path OFF (slow-path regression check — mandatory because the `find_module_for_type` determinism fix changes slow-path mangling too): core/hash 14/14, core/str 33/33, std/json 23/23, compiler/borrow 12/12. Determinism `determinism-gate.sh 10` (adversarial ON): **28/28 targets at or above their baseline floor**. Capture telemetry: 3264 methods + 1832 funcs deferred, 0 skipped. MEASURED NUMBERS (honest scope): bootstrap ~16min(eager, failing)→~34s(zero-body, working); per-file emitted IR ~22.5k→~3-8k lines; a rigorous full-suite cold-compile before/after was NOT run (would need the full suite twice; the per-suite runs above are the evidence base).

  (Superseded session-2 partial gate kept for history:) PARTIAL/PASS FOR SUB-FIXES. With blockers 1+2 landed and the fast-path OFF: core/hash 14/14, compiler/borrow 12/12, std/json 23/23, core/str 33/33, core/alloc 43/44 (miss = floating machine-load concurrency TIMEOUT, different test each run — not a regression). Determinism `determinism-gate.sh 10` (adversarial): 28/28 at floor (a 1/10 `tml_let_move_double_drop` dip re-confirmed 100% on re-run). The full fast-path gate (stdlib-emitted-once, per-EXE size drop, cold-compile before→after) is DEFERRED with the enablement (1.4/1.5/1.6).
- [x] 1.8 If full enablement cannot pass zero-divergence, land the codegen sub-fixes (1.2-1.4) that ARE clean independently (they fix K001s regardless), keep the fast-path off with an updated evidence-based comment, and document the precise residual — DONE. Landed 1.2 (Fix B) + 1.3 (dedup capture); 1.4 not guess-edited (needs fast-path repro to pinpoint the exact Range-value site). Updated the disabled-block comment in `testing_compile_parallel.cpp` to record blockers 1+2 FIXED / 3+4 remaining. Residual documented in `04-test-framework-performance.md` F-006/F-007, `docs/patches/v0.3.76.md`, and the memory file.

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation — DONE (session 1: F-006/F-007, VERSION 0.3.75→0.3.76, `docs/patches/v0.3.76.md`). SESSION 2 additionally: rewrote F-006 with the corrected model (blocker 3 misdiagnosed, blockers 3+4 unified, the two gating sub-blockers, measured 956s/449s, the `max_per_suite = 1` design constraint) and F-007 (now gated on one thing; also records that `generate_library_only.cpp` emits **no linkage attribute at all** today, which is the concrete F-007 work item); rewrote the disabled-block comment in `testing_compile_parallel.cpp` — it had been documenting a model now known to be wrong; VERSION 0.3.77→0.3.78; CHANGELOG row + `docs/patches/v0.3.78.md`.
- [x] 2.2 Write tests covering the new behavior — the acceptance set (clean suites) is the test surface, and SESSION 3 ran it in BOTH modes (fast-path forced ON and OFF, `--no-cache`) — that dual run IS the shared-stdlib behavior test: same suites, both code paths, zero divergence. The decls-only link-path tests belong with F-007 (future work). Note for a future hardening pass: a dedicated CI job running one suite with `TML_STDLIB_FASTPATH=1` would keep the fast path exercised on every change (today it only engages automatically on ≥32-file runs).
- [x] 2.3 Run tests and confirm they pass — 1.7 gate + determinism — DONE. SESSION 2 re-ran the acceptance set against a COLD (cleared) meta cache, which was mandatory: the `send_once` fix changes `Module::functions`, which is serialized into `.tml.meta`, and those caches are invalidated by *lib source* timestamps — NOT by compiler changes. Without clearing `build/debug/cache/meta` the fix silently appears to do nothing. Result: core/hash 14/14, core/str 33/33, std/json 23/23, compiler/borrow 12/12, core/alloc 43/44 (known floating concurrency miss; a first run showed 30/44 purely from concurrent background load and re-ran clean at baseline).

  `std/http` itself has 4 h2 `K001 integer constant must have integer type` failures. Assessed as PRE-EXISTING — documented as the phase27d `mut ref I64` breakage (`b12aeebc`, already in HEAD), which is why `std/http` sits outside the acceptance set. Stated confidence: documentary evidence plus the change being additive-only to a registration table. NOT an empirical bisect — a worktree build at HEAD would settle it definitively if anyone needs certainty.
