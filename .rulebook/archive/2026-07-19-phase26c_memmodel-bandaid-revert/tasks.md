# phase26c — Revert phase24 Band-Aids + Close the Bug Class (Stabilization ERA 0, Phase B gate)

> The proof that phase26b actually fixed the model is that the workarounds can
> ALL be deleted and everything still passes. Analysis:
> `docs/analysis/tml-table-analysis/04-self-hosting-and-strategy.md` (collateral tech debt).

> **RE-SCOPED 2026-07-17** (task predates two standing decisions): (a) `compiler-tml/`
> is **FROZEN** (user rule, 2026-07-16 — never anchor work or gates on cc_driver), so
> the ~70 `.duplicate()` partial-move band-aids in `compiler-tml/src/cc/` and the
> `essential.c --emit=ast` 100/100 gate are OUT of this task — phase26f 1.7 proved
> essential.c's 0% floor is a segfault in the frozen C frontend (pre-IR, not K001/K002);
> those reverts belong to the self-hosting era (phases 30–33) when the freeze lifts.
> (b) The `std/collections K001-free` gate DEPENDS on phase27a (the `{i64,i64}`-vs-`i32`
> re-inference family) — so phase27a executes FIRST; 26c closes the era-0 gate after.
> In-scope here: band-aids in the C++ compiler + `lib/core`/`lib/std` + the API-surface
> decision (1.3).

## 1. Implementation
- [x] 1.1 Inventory every phase24c–24n workaround still in tree WITHIN the non-frozen surface (2026-07-18). Findings: the `into_raw()`/`from_raw()` chains and `declarator_name_value_leak` were ALREADY deleted in phase24g (v0.3.45) — only comments referencing them remain. In-scope live band-aids: `Shared.get` still the old unsound bitwise copy + hazard docstring (shared.tml:118-150), `Shared.get_ref` on the mis-typing `ptr_as_ref` intrinsic, historical hazard/history comments in sync.tml/heap.tml/hashmap.tml/list.tml. C++ compiler side: ONLY the `ptr_read_clone` intrinsic infrastructure (intrinsic_table.cpp:21, llvm/builtins/intrinsics.cpp:663-675, mir/instructions_call.cpp:956-1049) — that is the phase26b FIX mechanism, not a band-aid → keep. Frozen compiler-tml/src/cc/ record: 180 `.duplicate()` partial-move calls (parser 89 / ast 55 / types 15 / preproc 21), 10 manual `impl Duplicate`, `CollectedArgs` dodge (preproc/macros.tml:352-458), 2 `get_clone` call sites (types.tml) — out of scope until the freeze lifts (phases 30–33).
- [x] 1.2 Revert the in-scope band-aids in dependency order, re-running the determinism corpus (adversarial) after each cluster
      - [x] lib/core + lib/std portion (2026-07-18): `Shared.get` folded to sound `ptr_read_clone` (Sync.get pattern); `Shared.get_ref` folded to zero-copy `ref (*this.ptr).value` borrow (dropped mis-typed `ptr_as_ref`); hazard/history docstrings removed in `alloc/shared.tml`, `alloc/sync.tml`, `alloc/heap.tml`; F-016/phase26b narrative comments shrunk in `collections/hashmap.tml` + `collections/list.tml`. Determinism corpus (adversarial ×10): 28/28 targets at/above floor. alloc (shared/sync/heap/refcount/weak/coverage/getmut) + collections (hashmap/list iter/retain/coverage) suites all green.
      - [x] C++ compiler portion: nothing to revert — per 1.1 inventory the only C++ artifacts are the `ptr_read_clone` intrinsic + its LLVM/MIR codegen, kept by the 1.3 decision as the canonical soundness mechanism (guarded by compiler/tests/compiler/get_deep_clone.test.tml).
- [x] 1.3 Simplify the library API surface: decided (2026-07-18) — fold soundness into now-sound `Shared.get` (uses `ptr_read_clone`), KEEP `get_clone`/`get_ref` as documented API (compiler-tml calls `get_clone`; `get_ref` is zero-copy path). All hazard docstrings describing the dead bitwise-alias bug class removed from `Shared`/`Sync`/`Heap`.
- [x] 1.4 Final gates (2026-07-19, all on the rebuilt compiler): determinism corpus adversarial ×100 → **28/28 targets at/above floors** (gate exit 0); `std/collections` consolidated → **96/96, btreemap/btreeset/arraylist K001-free** (phase27a's in-flight Class 2 fix at HEAD d39333dd satisfied the dependency — verified twice, pre- and post-rebuild); suites at/above baseline: core/alloc 44/45 (sole miss = the documented pre-existing floating X002/X003 single-test flaky, victim varies per run, green standalone), core/str + core/hash + std/json + compiler/borrow all green. NOTE: reaching this gate required fixing a genuine codegen soundness hole the new tests exposed — `ptr_read_clone[T]` was silently bitwise for user aggregates with handle fields but no explicit/derived Duplicate impl; fixed via structural clone-glue synthesis symmetric with drop-glue (intrinsics.cpp + derive/duplicate.cpp gen_structural_duplicate + stmt_let suppress_field_drops). The "standalone passes / suite fails" appearance was a harness FALSE-PASS (standalone dispatcher emits test_pass on panicking bodies) — filed phase44a_test-dispatcher-false-pass.
- [x] 1.5 Capture learning (2026-07-18): `rulebook_memory` learning "phase24 double-free class: why per-site band-aids never converged and how the model fix closed it" — root cause (bitwise reads aliasing nested handles, no move semantics), non-convergence proof (per-site fixes O(sites), each traded double-free for leak/imbalance; phase24l 29/30→25/30), structural close (ptr_read_clone + F-013 field reads + phase26f move dataflow), and the delete-the-band-aids-as-proof method.

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation (2026-07-19): the rewritten `Shared`/`Sync`/`Heap` docstrings ARE the API documentation (hazard framing → accurate sound-semantics docs); `docs/patches/v0.3.80.md` + CHANGELOG 0.3.80 row record the revert, the API decision, the codegen fix, and all gate numbers. No stale references remained in docs/specs/README (grep-verified).
- [x] 2.2 Write tests covering the new behavior (2026-07-18): `lib/core/tests/alloc/shared_get_sound.test.tml` — 3 regression tests pinning the sound `get`/`get_ref` semantics: (1) `get()` on a struct with a nested `Shared[I64]` field bumps the nested refcount to 3 and restores to 2 on the copy's drop (the phase24 double-free shape); (2) `Shared[Shared[I64]]::get()` same bump/restore on a directly-nested handle; (3) `get_ref()` zero-copy borrow leaves strong_count unchanged. All pass; existing shared/sync/heap alloc suites re-run green.
- [x] 2.3 Run tests and confirm they pass (2026-07-19): shared_get_sound green standalone AND suite-packed on the rebuilt compiler; full gate table in 1.4 (determinism ×100 28/28, collections 96/96, alloc/str/hash/json/borrow at baseline).
