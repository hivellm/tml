## 1. Implementation
- [x] 1.1 Audit `compiler/CMakeLists.txt` Release config: confirm every compiler target (`tml_types`, `tml_codegen`, `tml_mir`, frontend, plugins) gets `-O2`/`-O3` under Release with the Zig CC toolchain (ADR-007); fix any target that stays unoptimized — audited: Zig toolchain populates `CMAKE_C/CXX_FLAGS_RELEASE=-O3 -DNDEBUG`; verified in release build.ninja that ALL 40 targets' objects carry `-O3` (0 unoptimized); no CMakeLists fix needed (F-001 was purely the debug default)
- [x] 1.2 Build release via `cmd //c "scripts\\build.bat release"`; fix any release-only compile/link errors until it produces working `tml.exe` + plugin DLLs — clean success, 507/507 ninja steps, 0 errors (zig cc cache made it fast); produced `build/release/bin/tml.exe` + tml_compiler.dll (77 MB) + tml_codegen_x86.dll (52 MB) + tools/test/mcp plugins; ir-diff tool SKIP is pre-existing (also absent in debug bin)
- [x] 1.3 Sanity: run a targeted test suite with the release binary and confirm identical results vs debug (no optimization-induced miscompiles) — 49 files (core/hash + compiler/borrow + std/json) × 3 runs per binary: 49/49 passed on BOTH, identical; `check` diagnostics byte-identical (diff exit 0); pre-existing K001 failures (core/str, cc_*) reproduce byte-identically under both binaries (same ir:line:col) → deterministic, not optimization-induced
- [x] 1.4 A/B measure debug vs release: `tml check` trivial file + representative suite compile+run wall-clock; append numbers to `docs/analysis/tooling-performance/01-measurements.md` — cold path (no TML_DAEMON), perf_counter, n=7 interleaved: check trivial 396.3→213.0 ms (1.86×); check task.tml 5502→2703 ms (2.04×); 49-file suite compile+run median 66.60→31.52 s (2.11×); section "phase40b debug vs release" appended
- [x] 1.5 Document the daily-driver workflow (which binary MCP/daemon/agents use; debug reserved for C++ debugging); wire MCP/daemon to the release binary if the delta confirms the win — documented in 02-build-performance.md; MCP/daemon deliberately NOT flipped: binary choice is a hardcoded candidate order in `get_tml_executable()` (C++ change, not a config flip), the shared test cache (`build/debug/cache/tests.json`, keyed on the running binary's tml_compiler.dll fingerprint) invalidates ALL cached EXEs on every debug↔release switch, the default rebuild loop refreshes debug only (staleness footgun), and the phase40a daemon cache-hit path (~7 ms) doesn't exercise the compiler anyway; exact switch procedure documented instead
- [x] 1.6 GATE: release-vs-debug wall-clock delta recorded; targeted suite green on release binary — PASS: 1.86–2.11× recorded in 01-measurements.md; 49/49 green on release binary (3 independent runs)

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update documentation: `docs/analysis/tooling-performance/02-build-performance.md` (mark F-001 resolved with measured delta) + build docs — F-001 marked RESOLVED with 1.86–2.11× deltas + full daily-driver workflow section; README lever #4 marked done; patch notes `docs/patches/v0.3.68.md`; CHANGELOG + VERSION bumped to 0.3.68
- [x] 2.2 Write tests covering the new behavior (or waive: build-config change — covered by 1.3 suite run on release binary) — WAIVED per task text: no compiler/CMake source changed (audit found nothing to fix); behavior is validated by the 1.3 protocol (3× 49/49 green on the release binary, byte-identical diagnostics)
- [x] 2.3 Run tests and confirm they pass — final debug-binary run of the 3 sanity suites: 49/49 passed, exit 0 (also restores the shared test cache's debug fingerprint so concurrent debug sessions don't pay a full EXE-cache invalidation)
