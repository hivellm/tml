## 1. Implementation
- [x] 1.1 Baseline: run representative suites (the 49-file set: core/hash + compiler/borrow + std/json, plus one large suite e.g. core/str) in default per-file mode; record wall-clock, number of EXEs produced, and per-test results snapshot to `.sandbox/`; append numbers to `docs/analysis/tooling-performance/01-measurements.md` — 49-set per-file: 12.2/21.2/15.5 s (median 15.5 s, warm obj-cache), 49 links, 49/49 pass; core/str: 38.8 s, 32 links, 27 pass + 5 pre-existing K001 compile errors (str_coverage2/advanced/method/methods/transform, all `mem::replace` undefined-value); snapshots in `.sandbox/phase41a/res_*.json`; full-run discovery = 2066 files / 131 groups (not 1339 — cache count was stale)
- [x] 1.2 Research the aggregated paths before touching defaults — findings (file:line): (a) NDJSON per-test events preserved in run-all (dispatcher_gen.cpp:143-175,460-527; coordinator.cpp:364-445) BUT test_pass/test_fail emit sequential position `i` while test_start emits original index (dispatcher_gen.cpp:330/486 vs 362/372/513/523) → results misattributed when compile-failed files leave gaps; (b) path/name filtering happens on files BEFORE grouping (coordinator.cpp:72-118,726-738) — mode-independent; (c) per-test timeout watchdog lives IN the EXE (generate_entry.cpp:562-563 → essential.c:1240-1246 TerminateProcess(99)), coordinator attributes exit-99 to the started-but-unresolved test (coordinator.cpp:459-478) — preserved under aggregation, but subprocess budget hard-capped at 60 s regardless of file count (coordinator.cpp:243-244); (d) crash: pre-crash sibling results preserved (stdout parsed post-mortem), offender marked CRASH, never-started marked "NOT RUN" — NO re-run logic (coordinator.cpp:447-507); (e) per-file codegen failures isolated as SKIP + per_file_errors, dispatcher includes only compiled files (testing_compile.cpp:676-703,850-877), whole suite dropped only if ALL files fail or dispatcher/link fails (861-897) — no link-failure fallback. force_internal_linkage unconditional for test compiles (query_core.cpp:961-963,1019-1027). Unified: no result cache, parser handles only pass/fail (coordinator.cpp:929-955), link failure exits 0 with zero tests (silent loss) — disqualified as default
- [x] 1.3 A/B on the same suites: default vs `--suite-mode` vs `--unified` — DIVERGENCE FOUND AND ROOT-CAUSED: both aggregated modes fail every multi-file link with `lld-link: duplicate symbol: __tml_register_routes` — the HTTP route-registration function (and `__tml_route_thunk_N`) is always `define`d with external linkage in every test .obj (generate_entry.cpp:787, mir_codegen.cpp:926) even though all references are module-local (func.cpp:391-394 skips the declaration). Suite-mode timings invalid (links failed); unified linked-failed but exited 0 (silent). Winner: **suite-mode** after linkage fix (unified disqualified per 1.2(d,e) evidence); aggregation chunk raised to 25/EXE so the ≥10× gate is reachable (2066→176 suites = 11.7× with compiler_compiler aggregated; at 10/EXE only 4.4-7.4×)
- [x] 1.4 Flip the default for non-coverage runs (`cmd_test.hpp`/`cmd_test.cpp`); keep coverage forcing per-file; add/keep an explicit flag to force per-file mode; implement or verify compile-failure fallback — DONE (9 files): (1) `generate_entry.cpp`+`mir_codegen.cpp`: `__tml_register_routes`/`__tml_route_thunk_N` get `internal` linkage under force_internal_linkage (the 1.3 root cause); (2) `testing_dispatcher_gen.cpp`: pass/fail events now carry the original test index (4 sites); (3) `cmd_test.hpp/.cpp`: `suite_mode=true` default, 25 files/EXE non-coverage, coverage + `--no-suite` stay per-file; (4) `testing_discovery.cpp`: stale compiler_compiler forced-per-file exception removed; (5) `testing_coordinator.cpp`: compile-failed files pre-marked failed + seeded resolved (parity with per-file failure counting), crash/timeout offender attributed + never-started siblings re-run via `--test-index=N`, subprocess hard cap scales with file count, link-stage failure → suite split into per-file EXEs (`CompileResult::link_stage_failure` from `testing_compile.cpp`, fail-fast skips recoverable link failures in `testing_compile_parallel.cpp`); (6) idle suite-worker budget → intra-suite codegen threads (global ≤8 envelope unchanged) so targeted runs keep parallelism. NOTE: pre-flip full-run baseline attempt on the old binary CRASHED at ~1080/2066 after 1383 s (exit 0xC0000409, batch 28, right after `other_c_frontend` 60 s codegen timeout whose watchdog detaches the still-running codegen thread — pre-existing flakiness, phase27c family); the before/after full-run comparison is therefore done same-binary (new default vs `--no-suite`)
- [x] 1.5 Rebuild + verify parity — DONE: `lib/core/tests/hash` new-default vs `--no-suite` both 14/14 pass, identical (default = 1 aggregated exe `core_hash.exe`, per-file = 14 exes); `test_aggregation.sh` 16/16 (compiler/borrow 12→1 suite default vs 12 per-file/coverage; compile-failure SKIP isolation + crash/timeout offender attribution + never-started sibling re-run + aggregated-vs-per-file count parity all proven). Full-run same-binary A/B: pre-flip full run on the OLD binary crashed at ~1080/2066 (0xC0000409, pre-existing phase27c flaky — documented in 1.4), so parity is established same-binary (new default vs `--no-suite`) + per representative suite, not old-vs-new full run
- [x] 1.6 GATE — MET: link reduction ≥10× (full-run 2066 files → 176 suites at 25/EXE = **11.7×**, computed in 1.3; **14×** measured on core/hash: 14 exes → 1); zero result divergence (test_aggregation parity 16/16 + core/hash 14/14 identical both modes; pre-existing K001/flaky reproduce identically); wall-clock: per-suite level recorded, full-run rigorous A/B blocked by the pre-flip-binary phase27c crash (documented, not introduced by this change)

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation — `04-test-framework-performance.md` F-005 marked RESOLVED (11.7× link reduction, isolation/parity) + F-008/F-011 status; README lever #3 marked done; `docs/patches/v0.3.69.md` + CHANGELOG + VERSION → 0.3.69; 01-measurements phase41a appendix
- [x] 2.2 Write tests covering the new behavior — `compiler/tests/cli/test_aggregation.sh`: 16 checks (default aggregation, --no-suite per-file, --coverage per-file, compile-failure SKIP isolation, crash/timeout offender + sibling re-run, aggregated-vs-per-file parity)
- [x] 2.3 Run tests and confirm they pass — `test_aggregation.sh` 16/16; `core/hash` parity 14/14 both modes; determinism gate 28/28
