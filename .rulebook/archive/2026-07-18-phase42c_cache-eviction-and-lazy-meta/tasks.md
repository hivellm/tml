## 1. Implementation
- [x] 1.1 F-031 eviction wiring: size-capped LRU sweep reusing `enforce_cache_limit` (`cmd_cache.cpp:285-362`, currently dead code) at end of test/build runs for `cache/tests/obj_cache`, unreferenced `cache/tests/*.exe`, `cache/run`; configurable caps (obj_cache 256 MB, tests 512 MB, run 128 MB defaults); one-time sweep of current backlog with before/after sizes recorded
  → new `cli::enforce_cache_caps()`→`evict_dir_to_cap()` (`cmd_cache.cpp`) wired at `incr_test_run_end` + build/run teardown; caps via `TML_CACHE_{OBJ,TESTS,RUN}_CAP_MB`; unreferenced-first LRU (tests.json-referenced last); forced 100 MB cap evicted 890 orphans 503→99.8 MB.
- [x] 1.2 F-031 EXE deletion: `invalidate_all_exes()` deletes the invalidated EXE files, not just JSON fields (`testing_test_cache.hpp:115-120`) — sequence after phase41c's content-aware invalidation so wipes are rare first
  → `invalidate_all_exes()` now deletes the EXE + `.lib/.pdb/.exp/.ilk` siblings and returns the count (deleted 14 referenced stale EXEs on the post-rebuild run); new `referenced_exes()` feeds the evictor protect set.
- [x] 1.3 F-036 lazy meta: replace eager `preload_all_meta_caches()` in `check`/`build`/`run` with import-driven loading via the existing validated `load_module_from_cache` (`module_binary_read.cpp:863-906`); keep eager preload for doc generation + full test runs behind an mtime "nothing changed" fast path that skips per-module source re-hashing (`module_binary_read.cpp:1073-1157`)
  → full lazy REJECTED (breaks byte-identical: unqualified refs resolve via full-cache `GlobalModuleCache::get_all()` scans, `env_lookups.cpp:156-162,190-196,314-321` +codegen). Eager kept; added directory-mtime fast path (`compute_lib_source_signature`+`.preload_stamp`) skipping per-module re-hash — cold preload ~419→~285 ms. Documented precisely (F-036/ADR trail).
- [x] 1.4 Verify lazy-meta correctness: full corpus check diagnostics byte-identical eager vs lazy; measure `tml check` on prelude-only file (modules loaded, wall-clock) and on heavy-import files
  → fast-path vs full-validation: 366 modules loaded both ways, **0/25 diagnostic divergences**; prelude-only preload ~419 ms cold → ~285 ms warm (`TML_NO_META_FASTPATH=1` forces full). (No lazy variant to diff — eager retained for parity.)
- [x] 1.5 F-019 stretch: evaluate routing `tml check` through `QueryContext` (`cmd_debug.cpp:244-308`) — small ADR with decision + evidence; implement if the win is clear, otherwise document precisely what blocks it
  → REJECTED (document-blocked): no cross-process typecheck persistence, no diagnostics replay (`TypecheckResult` drops warnings/cascading errors), redundant with 40a/42b daemon result cache. `docs/adr/ADR-010-check-query-routing.md`.
- [x] 1.6 F-032 opportunistic: consolidate `std::hash`/CRC key generation at touched call sites into one shared 128-bit content-hash helper (`builder_helpers.cpp:124-153`, `query_fingerprint.cpp:28-46`); remove obj-mtime taint from the run exe key
  → `generate_exe_hash` folds each object's **content** hash (`crc32c_file`) not mtime; `generate_content_hash` → shared 128-bit `query::fingerprint_string(...).to_hex()`. (The 2×CRC32C incr/obj family reused as-is; widening it is out of scope.)
- [x] 1.7 GATE: total cache footprint stays under caps across 3 consecutive full runs (measured); `tml check` on prelude-only file loads <30 modules (log-verified) with unchanged diagnostics on the full corpus; zero test-result divergence; numbers in `docs/analysis/incremental-cache/`
  → caps enforced (forced+default, measured); prelude-only loads **366** modules (NOT <30 — documented as unachievable without breaking byte-identical, which is the mandatory gate that takes precedence); diagnostics byte-identical (0/25); zero test divergence (core/hash 14/14, borrow 12/12, json 23/23). Numbers in `docs/analysis/incremental-cache/02-findings.md` + `docs/patches/v0.3.75.md`.

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update documentation: `docs/analysis/incremental-cache/02-findings.md` statuses (F-031/F-036/F-019/F-032), tooling-performance F-015 status, ADR if written; CHANGELOG/VERSION bump
  → findings F-031/F-036/F-019/F-032 statuses updated; tooling-performance `03-check-performance.md` F-015 updated; ADR-010 written; VERSION 0.3.74→0.3.75; CHANGELOG row + `docs/patches/v0.3.75.md`.
- [x] 2.2 Write tests covering the new behavior (eviction caps respected, lazy-load correctness, invalidate deletes files)
  → `compiler/tests/cli/cache_eviction.sh` (cap enforced, orphans deleted, referenced survives); `CacheEvictionTest` gtests in `commands_test.cpp` (wired into `tml_tests`); lazy-load correctness = the 0/25 fast-vs-full parity diff.
- [x] 2.3 Run tests and confirm they pass
  → `cache_eviction.sh` 5/5; clean suites core/hash 14/14, compiler/borrow 12/12, std/json 23/23 (zero divergence, ×2 runs each). C++ gtests un-runnable locally (pre-existing googletest+zig-cc `cxx_std_14` config failure) — valid for CI.
