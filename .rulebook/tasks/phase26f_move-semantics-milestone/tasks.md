# phase26f — Move-Semantics Activation Milestone (the zero-cost enabler)

> The deferred, high-blast-radius core of the memory model (user decision 2026-07-16,
> rulebook decision #12). Activating real move semantics is what makes TML genuinely
> Rust-like/zero-cost — but it makes use-after-move a compile error and breaks every
> implicit-copy site (stdlib included), so it is a conscious milestone taken AFTER the
> concrete bugs are closed (phase26b Step 3). Foundation already in place: exported
> borrow facts (phase26b 2.1-2.3, v0.3.57, def-span join 100%) + accurate `initialized`
> state. Prereq: phase26b Step 3 (sound read-outs) landed, so container copies are the
> only remaining copy-double-free source.

## 1. Implementation
- [x] 1.1 Activate the checker's move dataflow: `move_value` / `move_projection` are now invoked at the real move sites (v0.3.60). **Wired (file:line-kind):** let-init ident + `let leaf = base.field` partial → `checker_stmt.cpp` `check_let`; by-value function args (honours `ref`/`mut ref` params via `TypeEnv::lookup_func`) → `checker_expr.cpp` `check_call`; by-value method args (receiver stays a borrow) → `check_method_call`; struct field inits + `..base` → `check_struct_expr`; enum-payload/constructor args → `check_call` (no free-func sig ⇒ by-value); `return` of a local → `check_return`; `x = y` → `check_binary`. **Prerequisite:** places were `nullptr`-typed (so `is_copy_type`⇒everything copy); type checker now records each `IdentPattern`'s resolved type in `expr_types_` (`types/checker/stmt.cpp` `bind_pattern`) and the checker threads it into places. Move classification = `!is_copy_type`. **Acceptance met:** join-proof (`TML_DROP_FACTS_DEBUG=1`) now shows `var=x moved_out=1 … DISAGREE-payoff` on `let y = x` (was uniformly 0); `refcount_bleed_probe` shows all `moved_out=0` (no false positives, `.duplicate()`/receivers correctly not moved).
- [x] 1.2 Use-after-move policy staged behind `TML_STRICT_MOVES=1` (read once, cached). **Default OFF** = facts populate, zero new diagnostics, zero behaviour change (all move-derived errors gated; `Moved` sticky through `drop_scope_places`; reassignment re-inits). **Flag-OFF regression:** determinism 22/22 adversarial, core/alloc 44/44, core/str 32/32, std/json 23/23, collections 19/19 (pre-existing `arraylist` K001, phase27a). **BLAST RADIUS (flag ON, 176 files): 53 move-errors / 7 files (4.0%)** — categorized in `specs/blast-radius/spec.md`: **~96% (51/53) idiomatic value-type/pointer copies** (`Layout` reused-by-value dominates = 42 errors across 4 alloc files, Rust's Layout is Copy but no derive; `Point`/`Interval` value structs reused; raw pointers `PtrType` not in `is_copy_type`), **only 1 genuine bug** (`double_free_probe` deliberate double-free). Classifier gaps recorded (NOT fixed): add `PtrType` to `is_copy_type`; auto-/derive-Copy for trivially-copyable value structs (~92% of radius); method-arg param-kind unresolved in checker. Fallout NOT fixed (that is items 1.3+/1.6). NEW blast-radius spec: `specs/blast-radius/spec.md`.
- [x] 1.2b Rust-faithful Copy classification — collapse the blast radius + make `moved_out` reliable (v0.3.61). **`is_copy_type` (`borrow/checker_core.cpp`):** (1) raw pointers `*T`/`*mut T` (`PtrType`) are Copy; (2) transparent named-type aliases are resolved (local table → module registry → global library cache, memoised) and classified by underlying type — handles `Ptr = *Unit` reused across allocator calls even when `core::ptr` is not imported; (3) Copy/Drop mutual exclusion enforced — a type with `impl Drop` NEVER classifies as Copy even given a stray `impl Copy`; (4) the existing `type_implements(name, "Copy")` consult now backed by stdlib derives. **Stdlib Copy derives (`@auto(duplicate)` + `impl Copy for T {}`):** `Layout` (core/alloc/layout), `Interval` (std/collections/interval_tree), `Point` (insertvalue test struct) — all trivially-copyable (all-primitive fields, no `Drop`). **Method-arg param-kind = SAFE-FALLBACK:** the checker no longer sets `moved_out` for bare-identifier method args (param-kind is unresolvable without method resolution; under-move is safe, over-move would leak in 1.3). Full fix (thread resolved method sigs into a MethodCallExpr side table) deferred — many type-checker resolution branches. **NEW BLAST RADIUS (flag ON, same 176 files): 0 errors / 0 files (was 53 / 7).** `double_free_probe` no longer flagged (raw pointer = Copy, Rust-faithful; still a real defect, just not the move checker's job). **Flag-OFF regression:** determinism 22/22 adversarial, core/alloc 44/44, core/str 32/32, std/json 23/23, `interval_tree` passes; the 4 collections K001s (`arraylist`/`class_coverage`/`collections_advanced`/`stack`) are pre-existing phase27a `{i64,i64}`-vs-`i32` family (proven independent by neutralise-and-recompile). **Join-proof:** `let y=x` moved_out=1, Layout-reuse moved_out=0, refcount_bleed unchanged. Updated `specs/blast-radius/spec.md` before/after.
- [x] 1.3 Step 2.4 swap (UNION design, v0.3.62): drive drop suppression from the now-live `moved_out` facts, **unioned** with the syntactic `consumed_vars_` set. **Impl:** new `LLVMIRGen::drop_suppressed_by_move(const DropInfo&)` (`drop.cpp`) = `consumed_vars_.count(name)` OR `ownership_by_span_[def_span].moved_out`; called at both `emit_scope_drops` and `emit_all_drops`. **Why UNION, not replacement:** the method-arg safe-fallback (1.2b) makes `moved_out` UNDER-report — by-value method args are unresolvable in the checker and tracked ONLY syntactically. So `consumed_vars_`/`has_consumed_fields`/the ~30 `mark_var_consumed` sites are **RETAINED**; the original "retire ~30 sites" (wiring-plan Step 5) is **deferred to the method-resolution follow-up** (thread resolved method sigs into a MethodCallExpr side table). **What the facts ADD over `consumed_vars_`:** precise ident moves the syntactic sites miss — plain `let b = a` let-init has no `mark_var_consumed` at the let handler (`consumed=0`) but is `moved_out=1`, so TODAY it was DOUBLE-DROPPED (control block decremented twice under a live owner). **Fix proven** by new canary `scripts/fixtures/let_move_double_drop_probe.tml` (`witness.strong_count()` 0→1 before/after), registered as `tml_let_move_double_drop` in `scripts/determinism-corpus.txt`. Partial moves: kept on the syntactic `"var.field"` prefix scan — the exported `PlaceOwnershipFact` carries only monotonic whole-place `moved_out` (granularity (i)); `moved_projections` NOT exported (deferred to Step 4), so no fact-side partial signal to union in. **Gate:** determinism **23/23** adversarial (incl. new canary + `refcount_bleed_userpath` 100%); core/alloc 44, core/str green, std/json green, collections at baseline (pre-existing `arraylist` K001 unchanged, independent of drops).
- [x] 1.4 Step 3.4 (v0.3.63): removed the container leak special-case in `emit_field_level_drops` (`drop.cpp`) that SKIPPED field drops for `Heap`/`List`/`HashMap`/`Buffer`/`BinaryWriter`/`BinaryReader` struct fields. **Now sound** because a struct moved via a tracked site (let-init/call-arg/struct-init/return/`..base`) has its SOURCE drop suppressed (1.3) → the surviving copy's field drop is the only one. **Skip FULLY REMOVED, not narrowed.** **RISK ANALYSIS (done before removal):** (a) by-value METHOD args (fact-side safe-fallback blind spot) — no double-free; adversarial `container_field_methodarg_probe.tml` (poison+quarantine) clean, its 2-leak residual identical before/after removal (pre-existing value-param drop gap, not introduced) — registered as `tml_container_field_methodarg` corpus canary (15/15 adversarial); (b) container read-outs + (c) iterator yields = balanced clones since Step 3 (safe); (d) raw `ptr_read[...]` in `lib/core+lib/std` only yields primitives (I8/I32/I64/F32/F64), never handle-bearing structs (safe). **LEAK-CLASS CLOSURE proof:** `container_field_leak_probe.tml` (Box{List[I64]} local, moved-into-field, dropped at scope exit) went **1000 leaks → 0**. **REGRESSION AUDIT:** `std/sync/mutex`+`once_barrier` timeouts are PRE-EXISTING — controlled round-trip (rebuild with skip RESTORED) reproduces the identical hangs; none of Mutex/RwLock/Barrier/Once/OnceLock has a container field; `once_barrier` documents a known barrier hang inline. These are the "known-failures" the gate excludes. **GATE:** determinism BOTH modes — adversarial **24/24** at/above floor (container-heavy F-002/013/016/022 + new double-free canary), non-adversarial 22/23 at 100% (sole exception legacy `essential.c`, documented 0% floor); core/alloc 44, core/str green, std/json green, core/sync green, collections at baseline (pre-existing phase27a K001 family), std/sync only the 2 pre-existing hangs; compiler sample 40/40 struct/enum/generic/drop/move/dyn + memory/drop 7/7 + partial_move_drop.
- [x] 1.5 Step 4 drop-flag elaboration (v0.3.64): conditional (control-flow-dependent) drops guarded by runtime `i1` drop-flag allocas. **Approach 1 (merge-point capture) — the checker merge WAS the capture point** (confirmed: `OwnershipState::Moved` is monotonic/sticky, never reset at branch boundaries; `save/restore_init_state` snapshot only `is_initialized`). **Checker (`checker_env.cpp`/`checker_expr.cpp`):** new `save_ownership_state`/`restore_ownership_state` + `merge_ownership_branches(pre, branches)` wired into `check_if` (both arms; implicit not-taken else = pre), `check_when` (per-arm), `check_loop`/`check_for` (body vs zero-iteration pre). Moved in ALL branches → static `Moved`; moved in SOME → `Moved` + new `conditionally_moved` bit. **`moved_out` semantics unchanged** (still set whenever moved on any path), so the 1.3 static-suppression path + canaries are preserved. Exported `conditionally_moved` on `PlaceOwnershipFact`. **Codegen (`drop.cpp`/`stmt_let.cpp`/`binary.cpp`):** `register_for_drop` allocates `alloca i1`=true for conditionally-moved bindings; `note_move_for_flag` stores false at every runtime move site (~30 `mark_var_consumed` sites + `let b = a` let-init ident); `note_reinit_for_flag` stores true on `x = new_value` reassignment; `emit_conditional_drop` guards scope-exit with `if (flag) { drop }`; `drop_suppressed_by_move` returns false for flagged bindings. Flags emitted ONLY where needed (zero-cost static path otherwise, Rust-faithful). **Canaries (adversarial corpus 15/15 each):** `condmove_branch`/`condmove_loop`/`condmove_when` (leak direction) + `condmove_taken` (double-free direction). Also verified out-of-band: partial-move-in-false-branch (no leak), reassign-after-taken-move re-arms flag (w2 dropped once). **GATE:** determinism **28/28** adversarial; no regression — core/alloc 44, str/json/sync green, borrow suite 12, memory/drop + partial_move_drop + 35/35 control-flow sample. **DEFERRED (not regressions, documented):** (i) projection-level conditional moves are NOT flag-guarded — `conditionally_moved` is whole-place (mirrors the phase26b Step 4 `moved_projections` deferral); tested partial-move case is sound today. (ii) drop-on-reassign for handle structs (`var a=X; a=Y` not dropping old X) is PRE-EXISTING (reproduced with no conditional move/flag), orthogonal. (iii) RefCell `replace`/`swap`: interior-mutability methods, not local-binding moves — outside the drop-flag scope; no canary needed (they don't create conditionally-moved local bindings). **DISCOVERED (pre-existing, orthogonal): flaky heap-corruption in module-path resolution** on some `tml run`/`build` files importing `core::alloc`+`std` (nondeterministic "Module '::alloc::::' not found"; reproduced on the pre-1.5 v0.3.63 binary → NOT from 1.5); low-frequency, canaries verified stable 15/15.
- [ ] 1.6 Migrate the fallout: every stdlib/user site that relied on implicit copy-and-reuse (the blast radius from 1.2) — insert explicit `.duplicate()`/borrow where a move now applies
- [ ] 1.7 Gates: `sig_alone.c`/`c_essential_repro.c`/`essential.c --emit=ast` all 100/100; full compiler+core+std suites at/above baseline; determinism adversarial + K002; leak-free under the adversarial allocator

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [ ] 2.1 Update or create documentation covering the implementation
- [ ] 2.2 Write tests covering the new behavior
- [ ] 2.3 Run tests and confirm they pass
