## 1. Implementation
- [x] 1.1 Write repro scripts FIRST (in `.sandbox/`, then promoted to tests in 2.2) for each hole — CONFIRMED on the pre-fix binary: F-027 garbage sidecar broke a valid `build` ("ast.bin deserialization failed"); F-028 warm daemon returned stale exit 0 (cold=1); F-030 `cache invalidate` left the `.ast.bin` on disk. F-029 runtime-confirmed to NOT affect project-local modules (they self-validate via source-hash) — its stale-result domain is stdlib-only, needing a forbidden lib/ edit; documented, fixed defensively.
- [x] 1.2 F-027 `.ast.bin`: fast-path moved into `provide_parse_module` (`query_core.cpp`) — records ReadSource dep + fingerprint, served from the query cache (no per-call re-deserialize), used only when sidecar ≥ source mtime; corrupt sidecar falls through to a real parse. `QueryContext::parse_module` now just `force()`s.
- [x] 1.3 F-028 daemon result cache: per-entry `universe_epoch` = max mtime over lib tree + each argv file's sibling dir (`cmd_daemon.cpp`); warm hit requires argv mtimes AND universe epoch unchanged. Lib sweep throttled ≤1×/750ms; argv-sibling scan un-throttled (immediate for project-local edits).
- [x] 1.4 F-029 daemon meta epoch: `preload_all_meta_caches` converted call_once → mutex + resettable guard; new `types::reset_meta_caches()` drops GlobalModuleCache + resets guard; daemon per-request lib-epoch probe (shared with F-028) resets meta + clears result cache on change.
- [x] 1.5 F-030 real `cache invalidate`: deletes `.ast.bin` sidecar + tests.json suite entries/EXEs (`TestResultCache::invalidate_source()`, respects phase41c compiler_hash) + incr entries/ir files (rewrites incr.bin with survivors), reporting exactly what was removed (to stdout). run/ + obj_cache/ documented as content-addressed/self-healing (clean --all only). `cache info`/`clean` extended to the whole cache tree.
- [x] 1.6 Rebuilt; all repros pass post-fix — `compiler/tests/cli/cache_staleness.sh` 6/6; representative suites result-identical.
- [x] 1.7 GATE: repros fail pre-fix / pass post-fix (documented in patch note); `cache invalidate <hash test>` removed 1 exe + 6 incr entries + 2 ir files (≥1 of each mappable class); zero divergence on core/hash 14/14, std/json 23/23, compiler/borrow 12/12.

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Updated `docs/analysis/incremental-cache/02-findings.md` (F-027..F-030 RESOLVED statuses), `05-mcp-warm-state.md` (staleness-correctness + residual-caveats section), CHANGELOG + VERSION 0.3.72→0.3.73 + `docs/patches/v0.3.73.md`.
- [x] 2.2 Promoted the four repros to `compiler/tests/cli/cache_staleness.sh` (consolidated, repo-convention shell test).
- [x] 2.3 Ran the CLI test — 6/6 pass.
