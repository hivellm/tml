## 1. Implementation
- [x] 1.1 Record baseline: `mcp__tml__check` + `emit-ir` latency on a trivial unchanged file (expect ~460 ms cold-subprocess) — append numbers to `docs/analysis/tooling-performance/01-measurements.md` — measured: check 452.8 ms median (n=7), emit-ir 842.9 ms median (n=5); appended "phase40a baseline" section
- [x] 1.2 Research + decide routing approach: (a) `execute_command` sets `TML_DAEMON=1` + auto-starts `tml_daemon.exe`, vs (b) in-process `tml_main` in `tml_mcp.exe` with `Loader::set_daemon_mode(true)` — read `cmd_daemon.cpp` protocol + `dispatcher.cpp:197-203` forwarding rules, record the decision and rationale in proposal.md — chose (a): per-child env block + throttled detached auto-start; rationale + staleness analysis recorded in proposal.md (note: the real forwarding hook is the thin launcher, pre-DLL-load; `tml_daemon.exe` is an unrelated HTTP proxy — the compile daemon runs as `tml.exe __daemon_server`)
- [x] 1.3 Implement the chosen routing for `check`, `compile`/`build`, `emit-ir`, `emit-mir` handlers (`mcp_tools.cpp`, `mcp_tools_codegen.cpp`) — `execute_command(cmd, timeout, use_daemon)` + warm-daemon helpers in mcp_tools.cpp; handle_check/handle_compile/handle_build/handle_emit_ir/handle_emit_mir opt in; handle_run deliberately excluded (program stdout would be lost in the daemon's NUL handles)
- [x] 1.4 Daemon lifecycle safety: MCP auto-(re)starts the daemon when absent; daemon staleness vs compiler DLL rebuild is detected (build.bat already kills daemon on rebuild — verify no stale-result window remains) — verified: build.bat:192 `taskkill /F /IM tml.exe` kills the compile daemon on every build (no locked DLLs, no stale window); second net: daemon self-checks DLL mtime per request; fixed launcher + cmd_daemon clients to treat the "compiler updated, restarting" notice as fall-through-to-direct-compile instead of spurious exit-1; also fixed dispatcher.cpp ordering bug that made daemon forwarding for `check` unreachable
- [x] 1.5 Rebuild (`scripts\build.bat`) and smoke-test every `mcp__tml__*` tool still returns correct results — build 36/36 OK; parity cold vs daemon verified for check (lib/core/hash.tml, range.tml: identical output, exit 0), emit-ir (identical .ll artifact), emit-mir + build (identical error output/exit on pre-existing working-tree K001 — same failure with no daemon involved); MCP end-to-end via stdio JSON-RPC: auto-start on first call, warm 26–31 ms, kill-mid-session → transparent restart verified
- [x] 1.6 GATE: `mcp__tml__check` on unchanged file < 50 ms (baseline ~460 ms) — **PASS: 6.6 ms median (n=10) at CLI level, ~28 ms through full MCP stack** vs 452.8 ms baseline (69×); warm-path numbers recorded in `01-measurements.md`

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update documentation: `docs/analysis/tooling-performance/05-mcp-warm-state.md` (mark F-016/F-017 resolved with measured numbers) + any MCP/daemon usage docs — F-017 RESOLVED + F-018 partially resolved with residual caveats in 05; F-016 RESOLVED in 03-check-performance.md; warm/baseline tables in 01-measurements.md
- [x] 2.2 Write tests covering the new behavior (daemon routing/fallback: daemon absent → auto-start; daemon killed mid-session → transparent restart) — `compiler/tests/cli/daemon_warm_path.sh`: 13 checks covering fallback-when-absent, warm parity + speed, hard-kill fallback, stale-DLL fall-through + daemon self-exit, MCP auto-start (drives tml_mcp.exe over stdio JSON-RPC)
- [x] 2.3 Run tests and confirm they pass — daemon_warm_path.sh 13/13 PASS; sibling regression mcp_timeout.sh 2/2 PASS
