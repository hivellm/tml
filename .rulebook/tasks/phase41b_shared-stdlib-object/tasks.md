## 1. Implementation
- [x] 1.1 Reproduce the historical failure — DONE. Scratch re-enable of `build_stdlib_object` (testing_compile_parallel.cpp:41-43) + minimal bootstrap engaged the fast path (state captured 20s, 1235KB IR); every test obj AND the shared object failed with `use of undefined value '@tml_N4core7runtime3mem7replaceE_R1T1T'`. Evidence in `.sandbox/repro_hash5.log`; summarized in `docs/analysis/tooling-performance/04-test-framework-performance.md` F-006.
- [x] 1.2 Root-cause each failure class — DONE (file:line, no guessing). (a) IN-SCOPE codegen gap: generic module free-function calls lack the call-site re-mangle+queue branch that generic `Type::method` calls have (`call_user.cpp:452` fallback emits the un-monomorphized `mem::replace[T]` template with no queued define; `Type::method` branch is `call_user.cpp:595-632`) — SAME error as the pre-existing 5× `core/str` K001 fails, i.e. a phase27a K001-family generic-substitution root (FROZEN). (b) i64/i32 range-iterator width = phase27a K001 Class 2 (FROZEN). (c) 12 phantom `pub mod` bootstrap imports (no source file) → typecheck aborts. (d) eager full-stdlib codegen times out >500s. (e) `generated_impl_methods_output_` not captured in CodegenLibraryState (I32::duplicate class).
- [~] 1.3 Fix root cause (stdlib once, external linkage, decls-only) — OUT-OF-SCOPE ROOT PROVEN. Requires the phase27a K001 generic free-function + range monomorphization fix (frozen) AND a per-suite-scoped/complete `build_stdlib_object` bootstrap redesign AND the impl-method dedup capture. Minimal in-scope slice landed: F-012 (1.5). Proposed path documented in `docs/patches/v0.3.70.md` + `04-test-framework-performance.md` F-006 for phase27a.
- [~] 1.4 Re-enable fast-path — NOT enabled: doing so regresses clean suites (core/hash 14/14 → 14 SKIP via the mem::replace K001 root), violating the zero-divergence gate. Left off with an accurate, evidence-based disable comment (`testing_compile_parallel.cpp`). Gated behind 1.3 / phase27a K001.
- [x] 1.5 F-012 — DONE. `compile_ir_string_to_object`/`_to_buffer` reuse one initialized `LLVMBackend`/`LLVMContext` per worker thread via `get_thread_local_llvm_backend()` (`compiler/src/cli/builder/object_compiler.cpp`), replacing per-object construct+initialize. Behaviour-transparent.
- [x] 1.6 Rebuild + verify — DONE. Rebuilt (fast-path off + F-012). Clean-suite parity, zero divergence: core/hash 14/14, compiler/borrow 12/12, core/str 27/32 (5 pre-existing K001 mem::replace, byte-identical), core/alloc 43/44 (shared_sync_edge pre-existing flaky, passes 1/1 alone). Per-EXE size unchanged (~345KB) since F-007 stays gated behind phase27a.
- [x] 1.7 GATE — DONE (to the in-scope extent). Wall-clock + baselines recorded in `01-measurements.md`; zero result divergence on representative clean suites; determinism 10× adversarial sentinels at floor (condmove_taken 10-run dip re-confirmed 20/20). Shared-stdlib "emit once" NOT achieved (proven out-of-scope, phase27a K001).

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Documentation — `04-test-framework-performance.md` (F-006/F-007 root-cause reproduced; F-012 RESOLVED), README lever #2, `01-measurements.md` (F-012 + baselines + parity), CHANGELOG + VERSION 0.3.70 + `docs/patches/v0.3.70.md`.
- [~] 2.2 Tests — tailWaiver: F-012 is a behaviour-transparent internal optimization (per-thread backend caching); it emits identical object bytes, so a bespoke unit test would have to expose internal thread-local state. Its correctness is the suite-parity in 1.6 (zero divergence) + the determinism gate. Shared-stdlib link-path / decls-only tests are not applicable (that path is not enabled — out-of-scope, phase27a).
- [x] 2.3 Run tests — DONE (1.6 parity + determinism gate).
