## 1. Implementation
- [x] 1.1 Telemetry first — `IncrTelemetry` (GREEN/RED + load/save count+time + ir GC) in `query_incr.hpp/.cpp`, `incr_telemetry_report()` logged per run via `incr_test_run_begin/end`; RED counted in `force()`, GREEN in `try_mark_green_codegen`. Measured GREEN≈0 baseline confirmed (finding), now 100% warm — numbers in `02-findings.md`.
- [x] 1.2 Session-scoped incr state — `get_shared_prev_session()` shared read-only `PrevSessionCache` per run (memoized path+mtime); `prev_session_` is `shared_ptr<const>`; `load_incremental_cache` uses the registry; save uses the shared prev for merge. Verified loads=4/saves=1 per suite (was per-file).
- [x] 1.3 Key stability (F-023) — removed `test_entry_index`+`has_cached_library_state` from `CodegenUnitKey` (`query_key.hpp`, serialization `query_incr.cpp`, v2→v3). Entry symbol + `s{id}_` prefix derive from `stable_test_symbol_id(file_path)` (`query_core.cpp`); dispatcher `symbol_id` (`testing_dispatcher_gen.*`, `testing_compile*.cpp`); `has_cached_library_state`→`options_hash`. Verified one-file touch = GREEN 13/14, RED 1.
- [x] 1.4 Content-based build hash (F-024) — `compiler_build_hash()` = CRC32C of DLL content, memoized in `<dll>.bhash` (mtime:size) sidecar (`query_incr.cpp`). `.bhash` created, verified.
- [x] 1.5 Config partitioning (F-025) + aging (F-021) — `incr.<opts>.bin` (`incr_cache_file_for`), `prune_incr_partitions` keeps newest 4; the `>10000` load cliff replaced by `write(max_entries=100k)` session-recency aging. Verified 2 bounded partitions.
- [x] 1.6 IR store GC (F-022) — `gc_ir_store()` deletes unreferenced `ir/*` at run teardown (`incr_run_teardown`, single-context + coordinator run guard); GREEN path no longer re-saves IR; v2→v3 bump reclaims old v2 IR on first teardown. ir/ now bounded to working set.
- [x] 1.7 Source-fingerprint memo (F-026) — `fingerprint_source()` memoized process-globally on (mtime,size) (`query_fingerprint.cpp`); benefits `compute_library_env_fingerprint` + GREEN ReadSource re-verification.
- [x] 1.8 GATE — one-file touch: GREEN=13 RED=1 (only affected RED); warm unchanged GREEN=14 RED=0; incr I/O loads=4/saves=1 per suite (was per-file O(N)); ir/ bounded + GC'd (finding's 2.2 GB was pre-reduced); zero divergence (hash 14/14, borrow 12/12, json 23/23; core/alloc `sync_*` = unrelated phase27d mut-ref regression); determinism 28/28 at floor. Numbers in `02-findings.md` + `docs/patches/v0.3.74.md`.

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Documentation — `02-findings.md` phase42a resolution (F-019..F-026), CHANGELOG 0.3.74 row, `docs/patches/v0.3.74.md`, VERSION 0.3.73→0.3.74. (ADR-002 has no standalone file; impl notes live in the findings doc.)
- [x] 2.2 Tests — `compiler/tests/cli/incr_cache.sh` (key stability on suite-membership touch, warm GREEN reuse, telemetry present, content-hash sidecar, config partitioning).
- [x] 2.3 Ran tests — `incr_cache.sh` 8/8 pass; clean suites pass; determinism 28/28.
