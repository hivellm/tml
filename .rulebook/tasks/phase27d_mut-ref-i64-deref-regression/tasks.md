> CORRECTED PREMISE (investigated 2026-07-18): this is NOT a regression and NOT
> caused by the other session. The `mut ref I64` errors are TYPE-CHECKER errors
> (T001/T056); the other session's uncommitted work is CODEGEN (method_impl/
> method_static_dispatch) + parser precedence, and codegen does not run during
> `tml check`. Verified: reproduces on the current binary; core files using `mut
> ref` do NOT show it. Root cause = a committed type-checker gap: BINARY operators
> do not auto-deref `ref`/`mut ref` operands, unlike assignment-through-mut-ref
> (expr_ops.cpp:95) and unary deref (expr_ops.cpp:219-248). std/http files are
> correct; the type checker is the bug.

## 1. Implementation
- [x] 1.1 Fix `check_binary_types` (expr_ops.cpp): added `deref_ref` lambda; both resolved operands now compare/print through `.inner` when RefType
- [x] 1.2 Fix RHS type propagation: `check_expr(*binary.right, deref_ref(left))` so literal RHS infers inner type, not the ref
- [x] 1.3 Fix binary result type: Add/Sub/Mul/Div/Mod/Bit/Shl/Shr arms `return deref_ref(left)`; PtrType arithmetic branches + comparison `make_bool()` preserved
- [x] 1.4 Rebuilt (EXIT=0). `check` app.tml (was 7× T001+T056 → 0) + parse.tml (was 3× T001 → 0), both exit 0 clean. match→matched fix present in working tree, will commit here
- [x] 1.5 GATE PASS: core/hash 14/14, compiler/borrow 12/12, std/json 23/23, core/str 33/33 (zero divergence); determinism 28/28 at floor (essential.c 0/10, c_essential_repro 8/10 = floors 0/80); app.tml+parse.tml clean. All 9 http files using `mut ref` now 0 "found mut ref"; 7/9 fully clean; worker.tml (4× L001 `#`) + dispatch.tml (2× T008 dup-var) have UNRELATED pre-existing residuals

- [x] 1.6 CODEGEN COUNTERPART DONE — both paths. **AST/legacy:** `mut ref` primitives already auto-deref'd in `gen_ident` (`expr/core.cpp:231`) but immutable `ref` did not → `icmp slt i32 %ptr, 100`. Immutable `ref` now derefs under a new OPT-IN flag `deref_ref_in_value_context_` (`llvm_ir_gen.hpp`), set by `gen_binary_ops` (`expr/binary_ops.cpp:39-50`) around a DIRECT identifier operand. Opt-in (not unconditional) is required: a bare `ref` local is routinely passed straight through to another `ref` param — `PartialEq`'s default `ne(this, other: ref Self) = not this.eq(other)` — and the method-call arg path has no `local_is_ref` bypass (only `call_user.cpp:806` free-function path does), so unconditional deref emitted a bogus `inttoptr` for ints and invalid IR for floats. **MIR (THIR→MIR):** same gap + 3 more, all previously unreachable since the construct never type-checked — (a) `build_binary` loads `RefType` operands (was `icmp sge ptr %count, %max`), (b) `build_binary` derefs the arithmetic RESULT type, since `MergeReturnsPass` types the unified-exit phi from `InstructionData.type` (`merge_returns.cpp:76`) so `ref I64` emitted `phi ptr` in an i64 fn, (c) `build_assign` stores THROUGH a `mut ref` binding instead of rebinding the SSA name (`bump(mut ref c, 10)` left `c` unchanged), (d) `build_unary` makes `mut ref <local>` yield the address OF THE VARIABLE by promoting it to alloca-backed (`mut_struct_vars`) on first address-taking — the generic ref path allocas a COPY so writes landed in a temporary — with `build_var` loading through that alloca for scalars too, guarded on the binding not itself being a `ref`. `PtrType` left intact throughout (genuine pointer arithmetic preserved), matching the checker

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Documentation done — VERSION 0.3.76→0.3.77, `docs/patches/v0.3.77.md` (documents BOTH halves: checker auto-deref + codegen load-through-ref), CHANGELOG row. Record corrected: phase27d was NOT a regression but a long-standing COMMITTED type-checker gap (`check_binary` had no ref unwrapping, unlike `check_unary` and the assignment-through-`mut ref` allowance beside it); the std/http sources were correct all along
- [x] 2.2 Write tests covering the new behavior — `compiler/tests/compiler/basics/mut_ref_value_context.test.tml` (mut ref: comparison + arithmetic + write-through; ref: read-only arithmetic)
- [x] 2.3 GATE PASS. (1) Fixture compiles + passes. NOTE: single-file test mode does not execute assertions (verified by negative control — a deliberately broken assert still "passes"; reproduced identically on the known-good sibling `let_else_basic.test.tml`, so it is a pre-existing harness granularity issue, not this fixture). Runtime therefore verified directly via `.sandbox/p27d_runtime.tml` on BOTH codegen paths (MIR + `--legacy`): all 6 values correct incl. caller-visible write-through. Fixture also passes under `--path=compiler/tests/compiler/basics` (82/82, then 367/376 in a broader collection). (2) 1.5 gate re-run, zero divergence: core/hash 14/14, compiler/borrow 12/12, std/json 23/23, core/str 33/33. (3) `determinism-gate.sh 10` → 28/28 at floor (a `condmove` target dips ~1 run in 2, different target each time — `tml_condmove_taken` then `tml_condmove_loop`; both pass 30/30 run directly, and a re-run gave 28/28). (4) `check` clean on app.tml + parse.tml, 0 "found mut ref"
- [x] 2.4 Pre-existing failures PROVEN unrelated (rebuilt with the change neutralized, i.e. `core.cpp`'s condition reverted to its exact pre-change form, then re-tested): (a) `cc_int_main` + `cc_with_stdio` fail with an identical `K001 use of undefined type named 'struct.EventEmitter'` at `ir:143`, in the type-definition preamble emitted before any function body — structurally unreachable from an in-body operand change; (b) `core/alloc` Shared/Sync instability — every run fails exactly ONE randomly-varying test (`shared_getmut`, `shared_refcount`, `shared_sync_edge`, `sync_get_accessors`, `shared_try_unwrap_weak`, `sync`, `shared_weak`) via `X002 TIMEOUT` or `X003` heap corruption; matched 7-run samples on both builds show the same distribution. This CORRECTS v0.3.74's changelog attribution of those `sync_*` timeouts to a "phase27d mut ref I64 regression"
