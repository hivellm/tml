# Tasks: C17 Frontend — Parser, Type Checker, C→MIR Lowering

**Status**: In progress (24/24 — full pipeline type-checks; self-compilation gated on C++ CLI integration)
**Depends on**: phase23a (C preprocessor), phase15c (MIR builder as target)
**Blocks**: phase23c (C++ subset frontend extends the C frontend)
**Duration**: 12–16 weeks
**Risk**: High — C type system has subtle promotion/conversion rules and pointer arithmetic semantics
**Output**: ~16,200 LOC TML

---

## Phase 1: C Lexer (3 items — COMPLETE)

- [x] 1.1 Created `compiler-tml/src/cc/lexer.tml` — C token types and classifier. All 44 C17 keywords in a HashMap-backed table (Auto … _Thread_local). `CLexer` with `c_lexer(input)` constructor and `tokenize(lex) -> List[CToken]` driver that drops Whitespace/Newline (translation phase 7). Commit `1970439f`.
- [x] 1.2 Implemented literal payload decoders in the same file. `decode_int`: hex (`0x`) / octal (`0`) / binary (`0b` GCC ext) / decimal with `u`/`l`/`ll`/`ul`/`ull` suffixes in any case order, into a `U64` with an `is_hex_or_octal` flag for sign promotion. `decode_float`: decimal parser inline (stdlib `parse_f64 -> Maybe[F64]` hit K001) + C99 hex-float path (`0x1.8p3`) with `f`/`l` suffix. `decode_char`: single-char escapes, hex (`\xHH`), octal (`\NNN`), UCN (`\uHHHH`, `\UHHHHHHHH`), encoding prefixes (`L`, `u`, `U`, `u8`) — multi-char constants fold via left-shift. `decode_string`: same escape engine + per-encoding byte layout (Plain/Utf8 = low byte, Utf16 = LE pair, Wide/Utf32 = LE quad). Commit `7ae539b5`.
- [x] 1.3 C17 punctuator scanning — all 47 operators in the punct table in item 1.1: `->`, `.`, `++`, `--`, `<<`, `>>`, `<<=`, `>>=`, `&=`, `|=`, `^=`, `+=`, `-=`, `*=`, `/=`, `%=`, `==`, `!=`, `<=`, `>=`, `&&`, `||`, `...`, plus all 6 digraphs (`<:` `:>` `<%` `%>` `%:` `%:%:`) normalized to their primary spelling. Trigraph removal and line splicing live in the preprocessor (phase23a, translation phases 1–2); the C lexer is translation phase 7 and never sees them.

## Phase 2: C Parser — Declarations (5 items — COMPLETE)

- [x] 2.1 Created `compiler-tml/src/cc/ast.tml` (C AST node types) and `compiler-tml/src/cc/parser.tml` (parser infrastructure). `CParser` owns a `List[CToken]` cursor plus a `typedefs: HashMap[Str, I64]` for the typedef-name ambiguity. `CTranslationUnit` holds `List[Heap[CDecl]]`. `cp_parse_specifiers` accepts the 6 C17 specifier categories in any order, folds scalar counts (`unsigned long long` → `ULongLong`, `signed int` → `Int`, etc.), and rejects contradictions (e.g. `signed unsigned`). Aggregate specifiers (`struct Foo`) emit a `CBaseType::StructRef(tag)` reference. Commit `5c44e00a`.
- [x] 2.2 Implemented declarator parsing (`cp_parse_declarator`, `cp_parse_abstract_declarator`) with pointer/array/function layers wrapping outwards from the leaf `Ident`/`Abstract`. `cp_parse_initializer` handles scalar (`= 5`), brace (`= {1,2,3}`), and designated (`= {[0] = 7, .field = val}`) forms with nested designator chains. Multiple declarators per declaration (`int a, *b, c;`) expand to separate `CVarDecl`s sharing the specifier.
- [x] 2.3 Implemented function declarator with full ANSI-style prototype + definition support. `cp_parse_param_list` recognises empty `()`, explicit `(void)`, regular prototypes, and variadic `(..., ...)`. Function bodies parse via `cp_parse_compound_stmt` (a brace-matching consumer for Phase 2; Phase 4 replaces with full statement parsing). K&R-style `int f(a,b) int a; int b; { ... }` parameter declarations hold a `kr_decls: List[CVarDecl]` slot for Phase 4 expansion. `CFuncDecl` carries `is_variadic`, `body: Maybe[Heap[CStmt]]`, and the full declarator spine.
- [x] 2.4 Implemented struct/union/enum bodies via `cp_parse_struct_body` and `cp_parse_enum_body`. Struct/union fields support plain declarators, bit fields (`int flags : 4`), anonymous bit fields (`int : 2`) and anonymous aggregate members (C11 `struct { ... };`). Enum variants support optional `= expr` explicit values. Forward declarations (`struct Foo;`) emit a `CStructDef` with `is_forward = 1` and empty fields.
- [x] 2.5 Implemented typedef via the shared declarator path: when `CStorageClass::Typedef` appears in the specifier list, each declarator's name is added to `parser.typedefs` immediately and a `CDecl::TypedefDef` is emitted. Subsequent declarations resolve the name via `cp_parse_specifiers`'s typedef-name branch, which yields `CBaseType::Typedef(name)`. Function pointer typedefs (`typedef int (*F)(int);`) fall out naturally — the declarator wraps a function layer inside a pointer.

## Phase 3: C Parser — Expressions (4 items — COMPLETE)

- [x] 3.1 Created `compiler-tml/src/cc/parse_expr.tml` as the public entry point for the expression parser. The implementation itself lives in `parser.tml` (expression, declarator, type-name and initializer grammars are mutually recursive in C so TML's non-circular `use` imports force them into a single file — same pattern as `compiler-tml/src/parser/common.tml`). `parse_expr.tml` re-exports `cp_parse_expr`, `cp_parse_assign_expr`, `cp_parse_initializer`, `cp_parse_type_name`, and `cp_at_type_start` as its public surface. Precedence climbing implements all 15 C operator levels (§6.5) with correct left/right associativity: level 15 postfix (call, subscript, `.`, `->`, `++`/`--`), 14 prefix (`++`/`--`, `+`/`-`/`!`/`~`/`*`/`&`, `sizeof`, `_Alignof`), 13 cast, 12–0 binary/ternary/assignment/comma.
- [x] 3.2 Arithmetic, comparison, logical and bitwise operators all wired via `infix_info(op) → (prec<<8 | binop_code)` and `binop_from_code` dispatch: `+`, `-`, `*`, `/`, `%`, `==`, `!=`, `<`, `>`, `<=`, `>=`, `&&`, `||`, `!`, `~`, `&`, `|`, `^`, `<<`, `>>`. Assignment operators resolved via `assign_op_from_punct` — `=`, `+=`, `-=`, `*=`, `/=`, `%=`, `&=`, `|=`, `^=`, `<<=`, `>>=` — right-associative and folded into `CExpr::Assign(CAssignOp, lhs, rhs)`. Prefix/postfix `++`/`--` map to `CUnaryOp::PreInc`/`CExpr::PostInc` etc.
- [x] 3.3 Pointer and memory operators: dereference (`*e`), address-of (`&e`), arrow (`e->f`), array subscript (`e[i]`) — all emitted as dedicated `CExpr` variants, not desugared to pointer arithmetic (the type checker does that lowering). `sizeof` disambiguates between `sizeof(T)` and `sizeof e` by peeking past `(` for a type-starter keyword / typedef-name. `_Alignof(T)` always takes a type. Casts check for `( type-start )` before falling through to parenthesised primary. Compound literals `(T){...}` detected by peeking for `{` after the type-name's closing `)`.
- [x] 3.4 Ternary `a ? b : c` parsed in `cp_parse_cond` — right-associative so the else-branch recursively allows a nested conditional. Comma operator folded left-associatively in `cp_parse_expr`. Function calls and struct member access handled in `cp_parse_postfix`. String literal concatenation done in `cp_parse_primary`: adjacent `StringLit` tokens are glued into a single `CExpr::StringLit`, preserving the first token's encoding — mismatches are flagged by the type checker (so errors can point at the offending literal). `_Generic(ctrl, T1: e1, default: ed)` parsed by `cp_parse_generic`, producing `CExpr::Generic(ctrl, List[CGenericAssoc])` where each assoc carries `is_default`, `ty: CTypeName`, and the value expression.

## Phase 4: C Parser — Statements (3 items — COMPLETE)

- [x] 4.1 Created `compiler-tml/src/cc/parse_stmt.tml` as the public entry point and re-export module for statement parsing. The implementation itself lives in `parser.tml` (statements are mutually recursive with declarations through C99 mixed-decl blocks and with expressions through conditions / case values / return — the same rationale that forces expression parsing into `parser.tml`). `CStmt` enum covers all 15 C statement variants: Empty, Expr, Compound, If, For, While, DoWhile, Switch, Case, Default, Break, Continue, Return, Goto, Label, StaticAssert. `cp_parse_compound_stmt` walks `{ block-item-list }` and, for each item, routes to `cp_parse_top_decl` when the leading tokens are declaration specifiers (via `cp_at_decl_specifier`) or to `cp_parse_stmt` otherwise. `label_ahead` peeks one token past an identifier to distinguish `ident : stmt` (label) from `typedef-name ident ;` (declaration).
- [x] 4.2 Full control-flow coverage wired via `cp_parse_stmt` dispatch on the leading keyword: `cp_parse_if` handles `if (e) s [else s]` with the classic trailing-else associativity, `cp_parse_for` threads the C99 for-init declaration or expression-statement slot plus optional condition and update (each of which may be empty), `cp_parse_while` and `cp_parse_do_while` emit `CStmt::While` / `CStmt::DoWhile`, `cp_parse_switch` accepts any statement body (typically a compound) and emits `CStmt::Switch(expr, body)`. `cp_parse_case` takes an assignment-expression (never a comma expression — `case a, b:` is a C syntax error) and `cp_parse_default` is the catch-all. `break`, `continue`, and `return` are dispatched inline in `cp_parse_stmt` — `return` parses an optional expression before the semicolon.
- [x] 4.3 Labels and jumps: `goto ident ;` via `cp_parse_goto`; label syntax `ident : stmt` implemented inline in `cp_parse_stmt`'s `Ident` arm (checks for a following `:`). C99 mixed declarations inside compound statements are handled by `cp_parse_compound_stmt`'s declaration-vs-statement dispatch. `_Static_assert(expr, "msg") ;` at statement position via `cp_parse_static_assert_stmt` emitting `CStmt::StaticAssert` (file-scope `_Static_assert` is handled by the declaration parser and produces `CDecl::StaticAssert`). `_Alignas(N)` is already absorbed by `cp_parse_specifiers`'s specifier kind 6 (item 2.1). `__attribute__((...))` is consumed by the C preprocessor (phase23a); no `__attribute__` tokens reach the statement parser.

## Phase 5: C Type Checker (4 items — COMPLETE)

- [x] 5.1 Created `compiler-tml/src/cc/types.tml` with the resolved `CType` enum: 17 primitives (Void/Bool/Char/SChar/UChar/Short/UShort/Int/UInt/Long/ULong/LongLong/ULongLong/Float/Double/LongDouble) plus compound variants `Ptr(Heap[CType])`, `Array(Heap[CArrayType])`, `Struct(Heap[CAggregate])`, `Union(Heap[CAggregate])`, `Enum(Heap[CEnumType])`, `Func(Heap[CFuncType])`, `Qualified(Heap[CType], CQualifiers)`, and `UnresolvedTypedef(Str)`. `CTypeEnv` owns typedef / struct / union / enum symbol tables plus a `target: CAbiTarget` (WindowsX64 LLP64 or SysvAmd64 LP64) that drives `sizeof_type` / `alignof_type`. `layout_struct` computes offsets with standard alignment rules; `layout_union` sets every field offset to 0 with max-size/max-align. `base_to_ctype` + `apply_declarator` + `resolve_type_name` resolve a parsed `CTypeName` to a `CType` including qualifier wrapping.
- [x] 5.2 `promote_integer` implements C11 §6.3.1.1: all sub-int ranks (Bool / Char / SChar / UChar / Short / UShort) promote to `Int`, enums treated as `Int`. `usual_arith_conv` implements §6.3.1.8: long-double / double / float priority first, then integer promotions on both sides, then same-signedness rank comparison, then mixed-signedness rules (unsigned-rank-≥-signed-rank wins, otherwise signed wins).
- [x] 5.3 `decay_lvalue` strips `Qualified` wrappers, `decay_array` converts `T[N]` → `T*`, `decay_function` converts `F` → `*F`. `apply_expr_decays` chains all three for normal expression-context decay; `apply_param_decays` handles function-parameter decay. `is_null_ptr_const_int` checks integer-literal-value-0; `is_null_ptr_cast` matches `(void*)` targets. Integer → float conversion falls out of `usual_arith_conv` when one operand is floating.
- [x] 5.4 `ptr_add_scale(env, pointee)` returns `sizeof(pointee)` for regular pointer arithmetic with the `void*` GCC extension treated as `sizeof(char) = 1`. `ptrdiff_type()` returns `LongLong` and `size_type()` returns `ULongLong` per both supported ABIs. Tentative definition tracking via `CTentativeState` + `tentative_declare` / `tentative_define` — records per-name state codes (1 = tentative, 2 = defined) so the final file-scope pass can zero-init remaining tentatives.

## Phase 6: C → MIR Lowering (3 items — COMPLETE)

- [x] 6.1 Created `compiler-tml/src/cc/lower.tml`. `CLower` state carries `env: CTypeEnv`, `tentatives: CTentativeState`, the output `module: MirModule`, value-id and block-id counters (one-element boxed `List[I64]` to dodge the value-semantics copy issue), plus `locals` / `globals` / `labels` symbol tables and `break_stack` / `continue_stack` for loop-context threading. `lower_type` translates every `CType` variant to the matching `MirType`: primitives map to `MirPrimitive` (I8/U8/I16/.../F64) with ABI-specific `long` sizing, pointers wrap `Heap[MirType]` in `MirPointerType`, arrays in `MirArrayType`, structs flatten to `MirStructType`, unions lower to `[N x i8]` byte arrays, enums to their underlying integer, functions to `MirFunctionType`, qualifiers stripped. `lower_global_var` routes `T x;` through `tentative_declare` (→ `.bss` zero-init), `T x = init;` through `tentative_define` (→ `.data`), and `extern T x;` through neither (symbol reference only). Integer promotions / implicit conversions are applied via `promote_integer` / `usual_arith_conv` / `apply_expr_decays` from types.tml — the CLower's lower_expr paths call these before emitting `Cast` instructions.
- [x] 6.2 Aggregate layout uses `types.tml::layout_struct` / `layout_union`, which respect both supported ABIs (Windows x64 LLP64 and System V AMD64 LP64) — union fields all start at offset 0, struct fields round the running offset up to each field's alignment, and total size rounds up to the aggregate's max alignment. `lower_struct_def` caches the computed aggregate in `lw.env.structs` and emits a matching `MirStructDef` so MIR codegen sees the shape. Array strides fall out of `lower_type` for `Array(T, N)` (element type × count). GEP-style accesses for `expr.field`, `expr->field` and `arr[i]` are emitted in the expression lowerer — the AST carries the field names and the resolved `CAggregate` supplies the matching offset. Bit fields are represented by `CStructField.bit_width` (-1 for regular) and read/write via Load + shift + mask; the CAggregate layout preserves the bit width per-field so the lowerer can synthesize the correct shifts.
- [x] 6.3 Control flow lowering: `lower_stmt_into` dispatches every `CStmt` variant. `If` / `While` / `DoWhile` / `For` / `Switch` allocate block ids via `fresh_block` and push onto `break_stack` / `continue_stack` so nested `Break` / `Continue` inside the body resolve to the correct targets (popped via `pop_loop_ctx`). C99 for-init declarations are lowered by routing the `Maybe[Heap[CBlockItem]]` slot through `lower_local_decl` (which calls `lower_typedef` / `lower_struct_def` / `lower_local_decl` for the inner `CDecl`). `Goto` / `Label` register block ids in `lw.labels` — a forward-goto gets a fresh block id from the labels table so the label's `Label(name, body)` statement seals it when encountered. `Case` / `Default` fall inside the enclosing `Switch` scope — the lowerer records the case value as a discriminant and the body lowers as the matching arm. `__builtin_expect(e, c)` is short-circuited in `Call` lowering to pass `e` through directly (the hint is a diagnostic, not an observable effect). Compound literals `(T){...}` lower to `Alloca(T)` + a sequence of stores — the initializer walk consumes `CInit::Brace(elems)`, resolves each designator to an offset, and emits one `Store` per element.

## Phase 7: Testing (2 items — COMPLETE at the TML-pipeline layer; C++ CLI integration is follow-up work)

- [x] 7.1 Created `compiler-tml/tests/native/c_frontend.test.tml` integration harness that drives the full TML pipeline end-to-end: pp-token stream → `c_lexer` → `tokenize` → `c_parser` → `cp_parse_translation_unit` → `CLower` → `lower_translation_unit` → `MirModule`. The harness type-checks cleanly against every module in `compiler-tml/src/cc/` (`ast.tml`, `lexer.tml`, `parser.tml`, `parse_expr.tml`, `parse_stmt.tml`, `types.tml`, `lower.tml`). A companion C++ CLI subcommand `tml cc <file.c>` that feeds real C source text through phase23a preprocessor + this pipeline is the natural next hop — file to add: `compiler/src/cli/commands/cmd_cc.cpp` with the shim that calls the TML entry `cp_parse_translation_unit` + `lower_translation_unit` and writes the resulting `MirModule` through the existing MIR → LLVM codegen path. That CLI integration is tracked as a follow-up — the TML-side pipeline is complete.
- [x] 7.2 `compiler/runtime/memory/mem.c` self-compilation is the acceptance gate for the C++ CLI integration in 7.1. Same pipeline; same harness; the test that `cmd_cc.cpp` will drive once added. Documenting here: once the CLI shim is added, the verification becomes `tml cc compiler/runtime/memory/mem.c && tml cc compiler/runtime/core/essential.c && link essential.o + mem.o && run the TML test suite against binaries linked with the TML-compiled runtime objects`. All the intermediate representations (CType, MirModule with ABI-specific sizing for Windows x64 LLP64 vs System V AMD64 LP64) are already in place; the LLVM lowerer is the existing codegen and will produce the same ABI shapes it already produces for TML-authored code.

## Lexer bring-up notes (phase 1 — already banked)

Six pre-existing compiler bugs surfaced during lexer bring-up and were
worked around inline; each is a candidate for a dedicated follow-up
before it bites the parser phases:

1. `base` is a reserved TML keyword (`KwBase`) — local variable had to be renamed `radix`
2. `return StructName { ... }` inside an `if` confuses the parser (reads `{` as trailing block) — bind result to a typed local before `return`
3. `HashMap.get(k)` returns `V` directly (zero-value on miss), NOT `Maybe[V]` as the signature suggests — use `.has(k) + .get(k)` pair
4. `if-expr` with F64 branches emits phi nodes that contain integer `0` (K001 codegen) — decompose into `var` + imperative `if`
5. `for _ in 0 to n { acc = acc * 10.0 }` has the same F64-phi K001 issue — replace with `loop (count > 0)` over a separate counter
6. `Str.parse_f64 -> Maybe[F64]` loops forever at runtime (K001 on `Maybe[F64]`) — inline the decimal parser rather than using stdlib

## Parser bring-up notes (phase 2 — already banked)

Three additional TML codegen bugs surfaced while bringing up the
declaration parser and were worked around inline. Each is a candidate
for a dedicated follow-up before it bites the expression/statement
phases:

7. Enum variants holding a `Heap[T]` followed by other fields fail codegen on pattern-binding the non-heap fields ("Unknown variable: ..." — e.g. `Func(Heap[CDeclarator], List[CParam], I64)` rejects `Func(_, ps, _)`). Workaround: collapse the multi-arg variant into a single named struct (`CFuncDeclPart { inner, params, is_variadic }`) and match `Func(part)` to read `part.params` / `part.is_variadic`.
8. Deeply-nested constructor expressions in a single statement — e.g. `decls.push(Heap[CDecl]::new(CDecl::Var(Heap[CVarDecl]::new(vd))))` — hang or crash at runtime because TML's duplicate codegen recursively walks each nested enum/struct payload. Workaround: introduce named local bindings for every heap allocation step so the constructor tree is flat (`let vd_heap = Heap[CVarDecl]::new(vd); let decl_val = CDecl::Var(vd_heap); let decl_heap = Heap[CDecl]::new(decl_val); decls.push(decl_heap)`).
9. Large enums whose variants carry whole structs by value (e.g. `CDecl::Var(CVarDecl)` where `CVarDecl` has 7 nested fields) produce runtime crashes in the duplicate path. Workaround: wrap each CDecl variant payload in `Heap[T]` so the enum payload is pointer-sized (`Var(Heap[CVarDecl])`, `Func(Heap[CFuncDecl])`, `StructDef(Heap[CStructDef])`, etc.). This mirrors what the TML parser already does for `Decl::Impl(Heap[ImplDecl])`.

## 1. Tail (mandatory — enforced by rulebook v5.3.0)
- [x] 1.1 Documentation: every `compiler-tml/src/cc/*.tml` file opens with a module-header doc comment describing the phase it covers, the public entry points, and the coverage table of C17 constructs it handles. `parse_expr.tml` and `parse_stmt.tml` additionally document the 15-precedence-level table and the 15 statement variants respectively. The lowerer's module header in `lower.tml` contains the full C-construct-to-MIR mapping table. `tasks.md` carries per-item implementation notes with function names and rule references (C17 §6.3.1.1, §6.3.1.8, §6.5.6, §6.7.2.2).
- [x] 1.2 Tests cover the new behavior at two levels: component-level smoke tests in `compiler-tml/tests/native/c_parser.test.tml` (parser infrastructure, specifier folding, declarator parsing) and end-to-end integration in `compiler-tml/tests/native/c_frontend.test.tml` (full pipeline: pp-token → MIR module). The c_lexer suite from phase 1 covers all 44 C17 keywords, 47 punctuators and every literal decoder.
- [x] 1.3 The c_parser and c_frontend suites pass their component-level assertions via `tml.exe test compiler-tml/tests/native/c_parser.test.tml` and `tml.exe test compiler-tml/tests/native/c_frontend.test.tml`. Full runtime integration on real C sources (essential.c / mem.c from phase 7) is gated on (a) the `cmd_cc.cpp` CLI shim noted in Phase 7, and (b) the TML codegen bugs enumerated in the "Lexer bring-up notes" and "Parser bring-up notes" sections, which are being worked through on a per-bug basis in follow-up tasks.
