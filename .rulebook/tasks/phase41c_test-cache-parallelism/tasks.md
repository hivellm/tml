## 1. Implementation
- [x] 1.1 F-014 diagnosis: root-caused with evidence — `compute_compiler_hash` used DLL `mtime:size` (`testing_test_cache.cpp:601-606`), so any relink/`touch` flipped the key and `invalidate_all_exes()` (`testing_coordinator.cpp:1039`) wiped every entry; measured: `touch` DLL → `compiler/borrow` 17.1s recompile vs 0.5s cached. 2nd defect: `--no-cache` saved an unloaded map → 319-entry tests.json reduced to 1.
- [x] 1.2 F-014 fix — persistence: coordinator now ALWAYS loads the cache before partitioning (even `--no-cache`), so `save()` preserves untouched suites; `--no-cache` only disables *using* the cache. Verified: `--no-cache` on 1 of 3 cached suites keeps all 3, sibling stays `is cached`. "Cache what passed, recheck what failed" preserved (is_cached gates all_passed; get_reusable_exe reuses unchanged exe for failed suites).
- [x] 1.3 F-014 fix — invalidation: `compute_compiler_hash` is now content-aware — `tml::VERSION` + CRC32C of DLL *content* (+size), memoised once/process. Verified: `touch` DLL → rerun STAYS cached (1.9s, no "invalidating" log) vs 17.1s recompile before. Correctness bias documented (content differs ⇒ invalidate).
- [x] 1.4 F-010: per-file incr saves batched into one per-suite flush — each file folds entries into a suite-local `IncrCacheWriter` accumulator (cheap suite-local lock), suite flushes `incr.bin` once under `g_incr_cache_mutex`. New `QueryContext::merge_incremental_into` + `IncrCacheWriter::merge_from_writer` (format-preserving). Serialization: O(files) → O(suites). Result-transparent (clean suites identical cold vs cached).
- [x] 1.5 F-013: each test file's `use` imports scanned ONCE up front into `TestImportScan{use_lines, package_names}`, threaded into both registry synthesis and package build.tml detection (was 2-3 re-reads). Superset of old behaviour; zero divergence.
- [x] 1.6 F-008/F-009: F-008 NOT raised — LLVM global-state blocker unchanged since phase41b deferred F-006 (documented in 04-...md + patch). F-009 done: per-file codegen wait uses a `condition_variable` (immediate wake, same 60s deadline) instead of 100ms polling; per-file thread is inherent to the single-threaded-codegen safety model (a timer wheel wouldn't remove it).
- [x] 1.7 GATE: 2nd consecutive run near-free — core/hash 22.9s→1.84s, compiler/borrow 18.5s→1.86s, std/json 49.0s→1.85s (all `is cached (passed, skipping)`), zero result divergence (14/14, 12/12, 23/23 both runs). touch-DLL now stays cached. Numbers in `docs/analysis/tooling-performance/01-measurements.md`.

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Docs: `04-test-framework-performance.md` F-008/F-009/F-010/F-013/F-014 statuses updated with numbers; measurements appended to `01-measurements.md`; VERSION 0.3.71→0.3.72, CHANGELOG row, `docs/patches/v0.3.72.md`.
- [x] 2.2 Tests: `scripts/tests/phase41c_test_cache.sh` — second-run reuse + parity, content-aware invalidation (touch), --no-cache sibling preservation, std-import result transparency.
- [x] 2.3 Ran tests: 4/4 pass (core/hash 14/14, compiler/borrow 12/12, std/json 23/23).
