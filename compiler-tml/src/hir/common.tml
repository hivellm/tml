//! HIR module root for the TML self-hosting compiler.
//!
//! Re-exports the core HIR types: HirExpr, HirStmt, HirPattern, HirModule,
//! and their associated enums and helpers.
//!
//! Phase 15a — item 1.1.

pub use compiler::hir::expr::*
pub use compiler::hir::stmt::{HirStmt, HirLetStmt, HirExprStmt}
pub use compiler::hir::pattern::{HirPattern, hir_pattern_type}
pub use compiler::hir::module::*
