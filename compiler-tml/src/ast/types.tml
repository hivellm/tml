//! Type expression AST nodes. See `ast/nodes.tml` for the canonical
//! definitions — all mutually-recursive AST types live in one file because
//! the TML module system does not yet support cross-module cycles.
//!
//! This file exists as a documentation anchor for the logical "types"
//! section of the AST, matching the C++ layout in `ast_types.hpp`.

use compiler::ast::nodes::*
