//! CLI module root — command-line interface for the TML self-hosted compiler.
//!
//! Provides subcommand dispatch (build, run, test, check, fmt, lint),
//! argument parsing, and diagnostic output.
//!
//! Phase 17b — item 1.1.

// --- CLI dispatcher ---
pub use compiler::cli::dispatcher::{dispatch, CliArgs, parse_args}
pub use compiler::cli::diagnostic::*
pub use compiler::cli::builder::{build_file, run_file, check_file}
