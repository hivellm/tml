//! MIR module root for the TML self-hosting compiler.
//! Phase 15c — item 1.1.

pub use compiler::mir::types::*
pub use compiler::mir::inst::{MirInst, InstructionData, MirConstant}
pub use compiler::mir::block::{BasicBlock, Terminator, block_new}
pub use compiler::mir::module::*
pub use compiler::mir::builder::core::{MirBuilder, builder_new, build_mir}
pub use compiler::mir::printer::{print_mir_module, print_mir_func}
