// Test only generic struct (no enum)
use test

type Pair[T] {
    first: T,
    second: T,
}

func main() -> I32 {
    let p: Pair[I32] = Pair { first: 10, second: 20 }
    if p.first == 10 and p.second == 20 {
        print("OK")
        return 0
    }
    print("FAIL")
    return 1
}
