use test

@packed
type PackedData {
    a: I32,
    b: I32,
    c: I32,
}

@test
func test_packed_compiles() -> I32 {
    let p = PackedData { a: 10, b: 20, c: 30 }
    assert_eq(p.a, 10, "packed a")
    assert_eq(p.b, 20, "packed b")
    assert_eq(p.c, 30, "packed c")
    return 0
}
