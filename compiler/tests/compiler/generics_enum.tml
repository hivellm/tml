// Test generic enum
use test

// Maybe[T] is a builtin type

func main() -> I32 {
    let m: Maybe[I32] = Just(42)
    when m {
        Just(v) => {
            print(v)
            return 0
        },
        Nothing => return 1,
    }
    return 1
}
