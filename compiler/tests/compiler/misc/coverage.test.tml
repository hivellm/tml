use test

// Test that the test framework basic coverage functions work
// Note: The coverage tracking functions are provided by the test runtime

@test
func test_basic_coverage_framework() -> I32 {
    // Simple test that verifies the test framework is working
    assert(true, "basic assertion should pass")
    assert_eq(1 + 1, 2, "1 + 1 should equal 2")
    return 0
}
