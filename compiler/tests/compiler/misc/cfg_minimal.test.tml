use test

// Minimal CFG test

@test
func test_simple_if() -> I32 {
    let x: I32 = 10
    let result: I32 = if x > 5 then { x + 1 } else { x }
    assert_eq(result, 11, "x > 5 should return x + 1 = 11")
    return 0
}
