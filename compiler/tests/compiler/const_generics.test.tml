// Test const generics
// Tests parsing and type checking of const generic parameters

// Basic const generic struct (parsing test)
type FixedArray[T, const N: I32] {
    value: T
}

// Test function to verify the syntax works
func test_const_generics() {
    // Verify parsing of const generics works
    // The struct definition above proves parsing is working
    let size: I32 = 10
    assert_eq(size, 10)
    println("Const generics parsing test passed!")
}

@test
func test_main() {
    test_const_generics()
}
