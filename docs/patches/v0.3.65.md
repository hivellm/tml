# v0.3.65 — Post-1.5 strict-moves fallout re-sweep: 0 (phase26f 1.6)

**Date:** 2026-07-16
**Phase:** phase26f (Move-Semantics Activation Milestone) — item 1.6
**ADR:** ADR-009 (B1-on-AST) · rulebook decision #12

## Summary

Item 1.6 ("migrate the fallout") is a **no-op**. After the drop-flag elaboration
(item 1.5, v0.3.64) the `TML_STRICT_MOVES=1` blast radius was re-measured over the
same four corpora and remains **0 move-errors / 0 files** — there is no
implicit-copy-and-reuse site left to migrate to `.duplicate()`/`ref`.

## Re-sweep

`TML_STRICT_MOVES=1 tml build <file> --emit-ir` over the corpus (grown 176 → 183
files with the phase26f 1.3/1.4/1.5 `scripts/fixtures/*` canaries):

| Metric | v0.3.61 | v0.3.64 (post-1.5) |
|---|---|---|
| Files compiled | 176 | 183 |
| Files with ≥1 move error | 0 | 0 |
| Total move-error lines | 0 | 0 |

Item 1.5 made branch move-tracking MORE precise (the `else`/other-arm branch now
starts from the pre-construct ownership rather than the sticky-`Moved` leaked from
the then-branch), so it can only REMOVE branch false positives, never add errors;
the monotonic `moved_out` verdict is unchanged, so after-construct uses flag
identically. Hence the radius cannot have grown, and the sweep confirms it did
not.

3 of the 183 files hit the **pre-existing, flaky module-path-resolution
heap-corruption bug** (nondeterministic "Module '::alloc::::' not found",
reproduced on the pre-1.5 v0.3.63 binary) — unrelated to move classification,
counted separately, not move errors.

## Verification

Re-measurement table appended to
`.rulebook/tasks/phase26f_move-semantics-milestone/specs/blast-radius/spec.md`.

## Not in this release

Item 1.7 (milestone gates) follows.
