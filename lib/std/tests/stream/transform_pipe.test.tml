// Tests for TransformStream: unpipe_all guard path
// NOTE: pipe/unpipe tests are in transform_pipe_unpipe.test.tml
use test::{assert}
use std::stream::transform::TransformStream

@test
func test_transform_unpipe_all() -> I32 {
    var ts: TransformStream = TransformStream::new()
    ts.unpipe_all()
    ts.destroy()
    return 0
}
