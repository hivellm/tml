use test::{assert_eq}

// BufferedReader and BufferedWriter are tested via standalone executables
// (.sandbox/test_buffered7.tml) because the module generates too much IR
// (~2500ms codegen) which exceeds the DLL test framework's stack limits,
// causing STACK_OVERFLOW/ACCESS_VIOLATION crashes when calling methods
// that access ByteStream internals.

@test func test_buffered_module_exists() -> I32 {
    // Placeholder test — full functionality verified via standalone
    assert_eq(1, 1)
    return 0
}