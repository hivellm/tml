// Tests for Json::get_or — blocked by method dispatch
// get_or is defined in impl Json but codegen can't find it
use test::assert

@test
func test_json_get_or_blocked() -> I32 {
    assert(true, "Json::get_or blocked by method dispatch codegen")
    return 0
}
