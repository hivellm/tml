// Tests for Json::get_or — blocked by Maybe[Json] pattern match codegen
use test::{assert}

@test
func test_json_get_or_placeholder() -> I32 {
    // get_or uses when-match on Maybe[Json] which produces LLVM type mismatch
    assert(true, "Json::get_or blocked by Maybe pattern match codegen")
    return 0
}
