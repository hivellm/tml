// Test Arc::duplicate with I32
use test::{assert, assert_eq}
use std::sync::Arc

@test
func test_arc_duplicate_i32() -> I32 {
    let arc1: Arc[I32] = Arc::new(42)
    let arc2: Arc[I32] = arc1.duplicate()
    // Both arcs should point to the same data
    return 0
}
