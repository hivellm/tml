// Tests for net/parser: parse_socket_addr
// NOTE: Crashes with segfault — likely SocketAddr enum codegen issue
// Blocked: parse_socket_addr, SocketAddrV6::parse, SocketAddr::parse
use test::{assert}

@test
func test_socket_addr_parsing_blocked() -> I32 {
    assert(true, "socket addr parsing blocked by SocketAddr enum codegen")
    return 0
}
