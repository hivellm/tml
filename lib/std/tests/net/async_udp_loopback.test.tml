// Tests for async_udp module
// BLOCKED: aio::timer_set has a parameter named `entry` which conflicts with
// the LLVM basic block label `entry:` in the generated IR.
// All async UDP tests require EventLoop which transitively imports timer_set.
// Error: "unable to create block named 'entry'" — compiler bug in parameter naming.
use test::{assert}

@test
func test_async_udp_blocked() -> I32 {
    assert(true, "async_udp tests blocked by timer_set parameter name codegen bug")
    return 0
}
