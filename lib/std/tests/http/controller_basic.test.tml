// Tests for Controller pattern
// NOTE: App::new() has a codegen bug when imported in test context (sext i32 in
// App::new mem_alloc calls). register_controller, register_controllers, ControllerInfo
// and register_all all trigger this. Placeholder until the codegen bug is fixed.
// Tracked: codegen bug in App::new() when imported standalone.
use test::{assert, assert_eq}

@test
func test_controller_module_placeholder() -> I32 {
    // Controller tests blocked by App::new() codegen bug in test context.
    // See h2_frame_min.test.tml pattern — real tests in separate file when bug fixed.
    assert(true, "placeholder")
    return 0
}
