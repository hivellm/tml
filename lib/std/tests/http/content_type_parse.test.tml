// Tests for content_type — parsing
use test::{assert, assert_eq}
use std::http::content_type::parse_content_type

@test
func test_parse_content_type_simple() -> I32 {
    let r = parse_content_type("application/json")
    assert_eq(r.0, "application/json", "json type")
    return 0
}
