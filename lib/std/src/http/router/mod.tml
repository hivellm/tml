//! Radix-tree URL router with pattern matching and parameter extraction.

pub mod router
pub use std::http::router::router::{Router, RouteMatch, NODE_STATIC, NODE_PARAMETRIC, NODE_WILDCARD, insert, node_new}
