// Tests for Array[U8, N]::is_ascii
//
// BLOCKED: Type-specialized const-generic impl dispatch.
// Methods from `impl[const N: I64] Array[U8, N]` (is_ascii, as_ascii,
// make_ascii_lowercase, make_ascii_uppercase, eq_ignore_ascii_case)
// are not resolvable — "Unknown method: is_ascii" at compile time.
// Root cause: const-generic monomorphization does not wire up impl methods
// for array literal syntax `[U8; N]`.
// Revisit when const-generic impl dispatch is implemented.
use test::{assert}

@test
func test_array_u8_is_ascii_blocked() -> I32 {
    // is_ascii() blocked — Unknown method from impl[const N: I64] Array[U8, N]
    assert(true, "is_ascii blocked by const-generic impl dispatch")
    return 0
}
