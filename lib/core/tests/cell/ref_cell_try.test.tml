// Tests for RefCell try_borrow, try_borrow_mut, and RefMut::replace
// BLOCKED: try_borrow/try_borrow_mut — Maybe[Ref[T]] codegen type mismatch
// BLOCKED: RefMut::replace — assertion fails, likely mut ref chain access codegen bug
use test::{assert}

@test
func test_ref_cell_try_blocked() -> I32 {
    assert(true, "try_borrow/try_borrow_mut blocked by codegen")
    return 0
}
