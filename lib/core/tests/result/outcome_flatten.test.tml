// BLOCKED: Outcome::flatten requires nested generic type codegen
// Outcome[Outcome[T, E], E] -> Outcome[T, E] triggers codegen failures
use test::{assert}

@test
func test_outcome_flatten_blocked() -> I32 {
    assert(true, "blocked by nested generic type codegen")
    return 0
}
