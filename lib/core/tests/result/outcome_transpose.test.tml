// BLOCKED: Outcome::transpose requires nested generic type codegen
// Outcome[Maybe[T], E] -> Maybe[Outcome[T, E]] triggers codegen failures
use test::{assert}

@test
func test_outcome_transpose_blocked() -> I32 {
    assert(true, "blocked by nested generic type codegen")
    return 0
}
