// Tests for core::pin — Pin[ref T] / Pin[mut ref T] methods
// BLOCKED: "Type mismatch: expected ref I32, found ref ref I32"
// Accessing a `ref T` field through an implicit `ref Self` receiver
// produces `ref ref T`, which the type system rejects.
// The 6 reference-based Pin methods remain uncovered until this is fixed.
use test::{assert}

@test
func test_pin_ref_blocked() -> I32 {
    assert(true, "Pin[ref T] methods blocked by ref-ref type mismatch")
    return 0
}
