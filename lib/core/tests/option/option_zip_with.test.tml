// BLOCKED: Maybe::zip_with triggers LLVM IR codegen bug
// "load operand must be a pointer to a first class type" for tuple struct
use test::{assert}

@test
func test_option_zip_with_blocked() -> I32 {
    assert(true, "blocked by tuple struct codegen bug")
    return 0
}
