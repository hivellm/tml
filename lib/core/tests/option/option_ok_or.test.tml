// Tests for Maybe::ok_or — blocked by generic param codegen
// ok_or[E] fails: %struct.E not monomorphized
// flatten fails: Maybe[Maybe[T]] struct size mismatch
// iter fails: MaybeIter[T] unsized type allocation
use test::{assert}

@test
func test_maybe_ok_or_placeholder() -> I32 {
    assert(true, "Maybe::ok_or blocked by generic codegen")
    return 0
}
