// Tests for Unit Display/Debug type formatting
use test::{assert, assert_eq}
use core::types::tuple::Unit
use core::fmt::Debug

@test
func test_unit_debug_output() -> I32 {
    let u = Unit::default()
    assert_eq(u.debug_string(), "()", "Unit debug output is '()'")
    return 0
}
