// Minimal test for binary helper
use test
use core::fmt::helpers::u8_to_binary_str

@test
func test_u8_binary_simple() -> I32 {
    let x: U8 = 5
    let s: Str = u8_to_binary_str(x)
    assert(s.len() > 0, "should produce output")
    return 0
}
