// Tests for core::iter::adapters::flatten — Flatten[I, U] adapter
// BLOCKED: IntoIterator trait dispatch - "Unknown method: into_iter"
// The flatten adapter requires I::Item: IntoIterator which doesn't resolve.
// Functions blocked: next, flatten (both)
use test::{assert}

@test
func test_flatten_blocked_codegen() -> I32 {
    assert(true, "flatten blocked by IntoIterator trait dispatch — see comment")
    return 0
}
