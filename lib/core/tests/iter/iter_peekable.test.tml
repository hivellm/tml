// Tests for core::iter::adapters::peekable — Peekable[I] adapter
// BLOCKED: Nested Maybe[Maybe[T]] codegen type mismatch in peeked field.
// The type checker now passes but LLVM IR generation produces:
//   Maybe[I32] vs Maybe[Maybe[I32]] type mismatch for the peeked field.
use test

@test
func test_peekable_blocked_codegen() -> I32 {
    assert(true, "peekable blocked by nested Maybe codegen — see comment")
    return 0
}
