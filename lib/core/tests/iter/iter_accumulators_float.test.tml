// BLOCKED: F32/F64 Sum/Product codegen bug - "ret float 0" (integer literal for float return)
// The compiler generates `ret float 0` instead of `ret float 0.0` for float accumulators
use test::{assert}

@test
func test_float_accumulators_blocked() -> I32 {
    assert(true, "blocked by float return codegen bug (ret float 0)")
    return 0
}
