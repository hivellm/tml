//! Iterator types and traits.
//!
//! This module provides the core iterator infrastructure for TML.
//!
//! # Core Traits
//!
//! - [`Iterator`] - The core iterator trait
//! - [`IntoIterator`] - Conversion into an iterator
//! - [`FromIterator`] - Building collections from iterators
//!
//! # Examples
//!
//! Basic iterator usage:
//!
//! ```tml
//! // Counter iterator
//! type Counter { current: I32, max: I32 }
//!
//! impl Iterator for Counter {
//!     type Item = I32
//!
//!     pub func next(mut this) -> Maybe[I32] {
//!         if this.current >= this.max {
//!             return Nothing
//!         }
//!         let val: I32 = this.current
//!         this.current = this.current + 1
//!         return Just(val)
//!     }
//! }
//! ```

pub mod traits
pub mod adapters
pub mod sources

// Re-export all from submodules
pub use traits::*
pub use adapters::*
pub use sources::*
